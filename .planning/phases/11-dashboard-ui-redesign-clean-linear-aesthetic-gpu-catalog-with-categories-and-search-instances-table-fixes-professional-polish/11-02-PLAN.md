---
phase: 11-dashboard-ui-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/src/app/cloud/gpu-availability/page.tsx
  - frontend/src/components/cloud/GPUAvailabilityTable.tsx
  - frontend/src/components/cloud/GPUCard.tsx
  - frontend/src/components/cloud/LaunchInstanceForm.tsx
  - frontend/package.json
autonomous: true
requirements:
  - UI-02
  - UI-03

must_haves:
  truths:
    - "GPU catalog displays category tabs (All, Blackwell, Hopper, Ada Lovelace, Ampere, Legacy) and clicking a tab filters the card grid to only show GPUs in that family"
    - "Search input filters GPU offerings by model name or region with debounced input (200ms) -- no lag during typing"
    - "GPU cards use the refined Linear aesthetic with flat buttons (btn-primary) instead of gradient-btn, consistent rounded corners, and muted accent colors"
    - "Launch modal uses btn-primary for the launch button, matches the new design language"
  artifacts:
    - path: "frontend/src/components/cloud/GPUAvailabilityTable.tsx"
      provides: "GPU catalog with category tabs, search bar, region filter, and card grid"
      contains: "classifyGPU"
    - path: "frontend/src/components/cloud/GPUCard.tsx"
      provides: "GPU card using btn-primary instead of gradient-btn"
    - path: "frontend/src/components/cloud/LaunchInstanceForm.tsx"
      provides: "Launch modal with btn-primary and refined styling"
  key_links:
    - from: "frontend/src/components/cloud/GPUAvailabilityTable.tsx"
      to: "frontend/src/lib/gpu-categories.ts"
      via: "import { GPU_CATEGORIES, classifyGPU }"
      pattern: "classifyGPU|GPU_CATEGORIES"
    - from: "frontend/src/components/cloud/GPUAvailabilityTable.tsx"
      to: "use-debounce"
      via: "import { useDebouncedCallback } from 'use-debounce'"
      pattern: "useDebouncedCallback"
---

<objective>
Redesign the GPU availability page into a proper catalog with GPU family category tabs and text search.

Purpose: Transform the flat GPU card grid into an organized, searchable catalog. Users currently see all GPUs in a flat list sorted by price, with only region filtering. After this plan, they can browse by GPU architecture family (Blackwell, Hopper, etc.) and search by name/region -- making it much faster to find the right GPU.

Output: Redesigned GPU availability page with category chip tabs, debounced search input, updated GPU cards with Linear aesthetic, and refined launch modal.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-RESEARCH.md
@.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-01-SUMMARY.md

@frontend/src/app/cloud/gpu-availability/page.tsx
@frontend/src/components/cloud/GPUAvailabilityTable.tsx
@frontend/src/components/cloud/GPUCard.tsx
@frontend/src/components/cloud/LaunchInstanceForm.tsx
@frontend/src/lib/gpu-categories.ts
@frontend/src/lib/types.ts
@frontend/src/app/globals.css

<interfaces>
<!-- Types from lib/types.ts -->
```typescript
export interface AvailableOffering {
  gpu_model: string
  vram_gb: number
  cpu_cores: number
  ram_gb: number
  storage_gb: number
  price_per_hour: number
  region: string
  tier: 'spot' | 'on_demand'
  available_count: number
  avg_uptime_pct: number
}

export interface GPUCardData {
  gpu_model: string
  vram_gb: number
  cpu_cores: number
  ram_gb: number
  storage_gb: number
  regions: string[]
  spot_price?: number
  on_demand_price?: number
  total_available: number
  offerings: AvailableOffering[]
}

export interface CreateInstanceRequest {
  gpu_type: string
  gpu_count: number
  region: string
  tier: 'spot' | 'on_demand'
  ssh_key_ids?: string[]
}
```

<!-- From lib/gpu-categories.ts (created in plan 01) -->
```typescript
export interface GPUCategoryDef {
  label: string;
  description: string;
  models: string[];
}

export const GPU_CATEGORIES: GPUCategoryDef[];
export function classifyGPU(gpuModel: string): string;
```

<!-- From components/cloud/EmptyState.tsx (created in plan 01) -->
```typescript
export function EmptyState({ icon, title, description, action }: EmptyStateProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install use-debounce and redesign GPU catalog with categories and search</name>
  <files>frontend/package.json, frontend/src/app/cloud/gpu-availability/page.tsx, frontend/src/components/cloud/GPUAvailabilityTable.tsx</files>
  <action>
1. **Install use-debounce**: `cd frontend && npm install use-debounce`

2. **Redesign GPUAvailabilityTable.tsx** -- Complete rewrite with catalog UX:

   **State:**
   - `searchQuery` (string) -- raw input value
   - `debouncedQuery` (string) -- debounced value for filtering
   - `activeCategory` (string) -- "All" | category label
   - `regionFilter` (string) -- region dropdown
   - `sortDir` ("asc" | "desc") -- price sort direction
   - `launchOffering` / `showLaunch` -- existing launch modal state

   **Filter toolbar layout** (single row, flex wrap):
   - **Search input** (left): A text input with a search icon (inline SVG magnifying glass, 14px, text-text-dim), placeholder "Search GPUs...", `type-ui-sm` styling, `bg-bg border border-border rounded-lg px-3 py-2 pl-9` (icon is absolutely positioned left). Width: `w-64` on desktop, full width on mobile. Use `useDebouncedCallback` from `use-debounce` with 200ms delay to update `debouncedQuery`.
   - **Category chips** (center): Horizontal scrollable row of pill buttons. Categories: `["All", ...GPU_CATEGORIES.map(c => c.label)]`. Active chip: `bg-bg-card-hover text-text rounded-md`. Inactive: `text-text-dim hover:text-text-muted rounded-md`. Each chip: `px-3 py-1.5 type-ui-xs font-medium whitespace-nowrap transition-colors`.
   - **Region filter** (right): Existing `<select>` but styled to match -- `bg-bg border border-border rounded-lg px-3 py-2 type-ui-xs text-text-muted`.
   - **Sort button** (right): Keep existing price sort toggle.

   **Filter chain** (in useMemo):
   1. Start with all on_demand offerings (existing behavior)
   2. If `activeCategory !== "All"`: filter using `classifyGPU(o.gpu_model) === activeCategory`
   3. If `debouncedQuery`: filter by `gpu_model` or `region` containing query (case-insensitive)
   4. If `regionFilter`: filter by exact region match
   5. Group into GPUCardData by gpu_model (existing logic)
   6. Sort by price (existing logic)

   **Results count**: Below the filter bar, show `{N} GPUs found` in `type-ui-2xs text-text-dim` if filters are active.

   **Empty state**: Use the new `EmptyState` component with a GPU icon, title "No GPUs match your filters", and action `{ label: "Clear filters", onClick: clearFilters }`.

   **Skeleton cards**: Keep existing SkeletonCard but update `rounded-xl` to use the new standardized border radius.

3. **gpu-availability/page.tsx** -- Update page heading to "GPU Catalog" (more descriptive than "GPU Availability"). Keep the simple layout wrapper.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npm run build</automated>
  </verify>
  <done>GPU availability page shows category chip tabs, search input with debounced filtering, results count indicator, and EmptyState for no-match scenario. Typing in search filters after 200ms delay. Clicking category tabs filters GPU cards by architecture family.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign GPU card and launch modal with Linear aesthetic</name>
  <files>frontend/src/components/cloud/GPUCard.tsx, frontend/src/components/cloud/LaunchInstanceForm.tsx</files>
  <action>
1. **GPUCard.tsx** -- Visual redesign with Linear aesthetic:
   - Card container: `bg-bg-card border border-border rounded-[10px] p-5 hover:border-border-light transition-colors` (use standardized radius, remove `group` class if not needed).
   - GPU model name: `type-ui-sm text-text font-medium` (not h5/bold -- Linear uses weight/opacity hierarchy, not large headings on cards).
   - VRAM badge: change from `bg-purple-dim text-purple-light` to more muted `bg-bg-card-hover text-text-muted` pill -- reduce color noise.
   - Specs row: Keep the CPU/RAM/Storage labels but tighten spacing. Use `gap-3` instead of `gap-4`.
   - Price display: keep mono font but use `text-text` not `text-text` (verify contrast). Show price without the "Price" label header -- just the amount prominently.
   - Region tags: keep existing rounded-full pills with `bg-bg-card-hover text-text-dim`.
   - Availability count: keep color coding (green >5, amber >0, red 0).
   - **Launch button**: Replace `gradient-btn` with `btn-primary` class. Keep `px-4 py-1.5 rounded-lg type-ui-xs font-medium`.
   - "Unavailable" text: keep as `text-text-dim`.

2. **LaunchInstanceForm.tsx** -- Visual refinement:
   - Modal card: update `rounded-xl` to `rounded-[10px]` for consistency.
   - Header: keep layout but use `type-ui-sm font-medium` for "Launch Instance" (smaller, more refined than current type-h4).
   - Pre-filled mode specs grid: keep but standardize `rounded-[10px]` on the grid container.
   - Price confirmation: change `text-purple-light` to `text-text` for the price, with `type-h4 font-mono` (slightly smaller). Keep monthly estimate in `text-text-dim`.
   - **Submit button**: Replace `gradient-btn` with `btn-primary`. The disabled state: `bg-bg-card-hover text-text-dim cursor-not-allowed`.
   - Cancel button: use `btn-secondary` class (from plan 01).
   - Input focus rings: change from `focus:ring-purple/50 focus:border-purple/50` to `focus:ring-1 focus:ring-border-light focus:border-border-light` for a more subtle, non-purple focus state matching Linear aesthetic.
   - Error alert: keep red styling, no change.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npm run build</automated>
  </verify>
  <done>GPU cards use btn-primary instead of gradient-btn, have consistent 10px border radius, muted VRAM badges, and refined typography. Launch modal uses btn-primary/btn-secondary buttons with subtle focus rings and standardized border radius.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `grep -c "gradient-btn" frontend/src/components/cloud/GPUCard.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/components/cloud/LaunchInstanceForm.tsx` returns 0
- `grep -c "classifyGPU" frontend/src/components/cloud/GPUAvailabilityTable.tsx` returns at least 1
- `grep -c "useDebouncedCallback" frontend/src/components/cloud/GPUAvailabilityTable.tsx` returns 1
- `grep "use-debounce" frontend/package.json` shows dependency installed
</verification>

<success_criteria>
1. GPU catalog shows horizontal category tabs (All, Blackwell, Hopper, Ada Lovelace, Ampere, Legacy)
2. Clicking a category tab filters the card grid to only matching GPU families
3. Search input filters by GPU model name and region with 200ms debounce
4. GPU cards use flat btn-primary button, muted VRAM badges, consistent border radius
5. Launch modal uses btn-primary/btn-secondary, refined focus states
6. Empty state when no GPUs match uses shared EmptyState component
7. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-02-SUMMARY.md`
</output>
