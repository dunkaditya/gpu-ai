---
phase: 11-dashboard-ui-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - frontend/src/components/cloud/InstancesTable.tsx
  - frontend/src/components/cloud/InstanceDetail.tsx
  - frontend/src/components/cloud/BillingDashboard.tsx
  - frontend/src/components/cloud/SSHKeyManager.tsx
  - frontend/src/app/cloud/instances/page.tsx
  - frontend/src/app/cloud/ssh-keys/page.tsx
  - frontend/src/app/cloud/billing/page.tsx
  - frontend/src/app/cloud/settings/page.tsx
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Instances table columns align properly with headers -- no colSpan hack, proper column widths, SSH command not truncated to 260px"
    - "Instances table uses CSS grid (div-based) layout for clickable rows with proper column alignment"
    - "All dashboard pages use consistent btn-primary/btn-secondary buttons instead of gradient-btn"
    - "Empty states across all pages use the shared EmptyState component for visual consistency"
    - "All cards use standardized 10px border radius, all pages have consistent spacing"
  artifacts:
    - path: "frontend/src/components/cloud/InstancesTable.tsx"
      provides: "CSS grid-based instances table with proper column alignment and full-row clickable links"
    - path: "frontend/src/components/cloud/InstanceDetail.tsx"
      provides: "Instance detail page with btn-primary, standardized card radius"
    - path: "frontend/src/components/cloud/BillingDashboard.tsx"
      provides: "Billing page with btn-primary, consistent styling"
    - path: "frontend/src/components/cloud/SSHKeyManager.tsx"
      provides: "SSH key manager with btn-primary, EmptyState, consistent styling"
  key_links:
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "CSS grid layout"
      via: "grid-template-columns with named columns"
      pattern: "grid-template-columns|contents"
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "frontend/src/components/cloud/EmptyState.tsx"
      via: "import EmptyState"
      pattern: "EmptyState"
---

<objective>
Fix the instances table column alignment using CSS grid, and apply professional polish across all remaining dashboard pages (instances, billing, SSH keys, settings).

Purpose: The current instances table uses a `colSpan={8}` hack with a single `<Link>` wrapping the entire row, causing column misalignment and SSH command truncation. Switching to CSS grid with `display: contents` on `<Link>` elements fixes alignment while preserving clickable rows. Additionally, all remaining pages need to migrate from gradient-btn to btn-primary and use the shared EmptyState component for consistency.

Output: Fixed instances table, polished billing dashboard, SSH key manager, settings page, and instance detail -- all using the refined Linear aesthetic.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-RESEARCH.md
@.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-01-SUMMARY.md

@frontend/src/components/cloud/InstancesTable.tsx
@frontend/src/components/cloud/InstanceDetail.tsx
@frontend/src/components/cloud/BillingDashboard.tsx
@frontend/src/components/cloud/SSHKeyManager.tsx
@frontend/src/app/cloud/instances/page.tsx
@frontend/src/app/cloud/ssh-keys/page.tsx
@frontend/src/app/cloud/billing/page.tsx
@frontend/src/app/cloud/settings/page.tsx
@frontend/src/lib/types.ts
@frontend/src/components/cloud/EmptyState.tsx

<interfaces>
<!-- Key types -->
```typescript
export interface InstanceResponse {
  id: string
  name?: string
  status: 'starting' | 'running' | 'stopping' | 'terminated' | 'error'
  gpu_type: string
  gpu_count: number
  tier: 'spot' | 'on_demand'
  region: string
  price_per_hour: number
  connection?: ConnectionInfo
  error_reason?: string
  created_at: string
  ready_at?: string
  terminated_at?: string
}

export interface ConnectionInfo {
  hostname: string
  port: number
  ssh_command: string
}

export interface SSHKeyResponse {
  id: string
  name: string
  fingerprint: string
  created_at: string
}

export interface UsageResponse {
  sessions: BillingSessionResponse[]
  total_cost: number
  currency: string
}

export interface SpendingLimitResponse {
  monthly_limit_cents: number
  monthly_limit_dollars: number
  current_month_spend_cents: number
  current_month_spend_dollars: number
  percent_used: number
  billing_cycle_start: string
  limit_reached_at?: string
}
```

<!-- EmptyState component from plan 01 -->
```typescript
export function EmptyState({ icon, title, description, action }: {
  icon: React.ReactNode;
  title: string;
  description?: string;
  action?: { label: string; href?: string; onClick?: () => void };
}): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix instances table with CSS grid and polish instances pages</name>
  <files>frontend/src/components/cloud/InstancesTable.tsx, frontend/src/components/cloud/InstanceDetail.tsx, frontend/src/app/cloud/instances/page.tsx</files>
  <action>
1. **InstancesTable.tsx -- Replace HTML table with CSS grid for proper column alignment**:

   **DesktopTable rewrite** using CSS grid pattern from research:
   - Container: `<div className="hidden md:block">` wrapping a grid-based table.
   - Grid: `<div className="grid" style={{ gridTemplateColumns: "minmax(160px, 1.5fr) minmax(100px, 1fr) 100px 100px 100px 100px minmax(200px, 2fr) 80px" }}>` -- 8 columns matching the 8 headers (Name, GPU, Status, Region, Cost, Uptime, SSH Command, Actions).
   - **Header row**: Use `<div className="contents">` wrapper, each header cell as a `<div>` with `px-4 py-3 type-ui-2xs text-text-dim font-medium uppercase tracking-wider border-b border-border`.
   - **Data rows**: Each row is a `<Link href={...} className="contents group">` wrapping individual cell `<div>`s. The `contents` display makes the Link transparent to grid layout while preserving the link behavior across all cells. Each cell div: `px-4 py-3 flex items-center border-b border-border/30 group-hover:bg-bg-card transition-colors`.
   - **SSH Command column**: Remove the `max-w-[260px] truncate` constraint. Instead use `min-w-0` on the cell and `truncate` on the code element, allowing it to use all available space in the `minmax(200px, 2fr)` column. This fixes the SSH truncation bug.
   - **Name column**: EditableName component stays the same but gets `onClick stopPropagation` to prevent navigation when clicking to edit.
   - **Actions column**: Terminate button stays the same with `onClick stopPropagation/preventDefault`.
   - Keep all existing functionality: EditableName, CopyButton, formatUptime, StatusBadge usage.

   **MobileCards**: Keep existing card layout but update `rounded-lg` to `rounded-[10px]`.

   **Empty state**: Replace the inline empty state JSX with the shared `EmptyState` component. Icon: server/stack SVG (keep existing). Title: "No instances running". Description: "Launch your first GPU instance to get started." Action: `{ label: "Browse GPUs", href: "/cloud/gpu-availability" }`.

   **Migrate buttons**: Replace `gradient-btn` on the "Browse GPU Availability" link in empty state with `btn-primary`.

2. **InstanceDetail.tsx -- Apply Linear aesthetic polish**:
   - Card radius: Change all `rounded-xl` on InfoCard to `rounded-[10px]`.
   - "Launch New Instance" button in terminated state: Replace `gradient-btn` with `btn-primary`.
   - Terminate button: keep red styling (not btn-primary -- it's a danger action).
   - Back link: keep existing chevron but ensure `text-text-dim hover:text-text` (already correct).
   - SSH command box: keep the bg-bg rounded-lg code display but update to `rounded-[10px]`.

3. **instances/page.tsx -- Minor polish**:
   - "Launch Instance" header button: Replace `gradient-btn` with `btn-primary`.
   - Wrapper card: Update `rounded-lg` to `rounded-[10px]`.
   - Loading skeletons: Update rounded corners to match.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npm run build</automated>
  </verify>
  <done>Instances table uses CSS grid with proper column alignment, SSH command column has flexible width (not truncated to 260px), all buttons migrated from gradient-btn to btn-primary, empty state uses shared EmptyState component, consistent 10px border radius throughout.</done>
</task>

<task type="auto">
  <name>Task 2: Polish billing, SSH keys, and settings pages</name>
  <files>frontend/src/components/cloud/BillingDashboard.tsx, frontend/src/components/cloud/SSHKeyManager.tsx, frontend/src/app/cloud/ssh-keys/page.tsx, frontend/src/app/cloud/billing/page.tsx, frontend/src/app/cloud/settings/page.tsx</files>
  <action>
1. **BillingDashboard.tsx -- Visual polish**:
   - Summary cards: Update `rounded-xl` to `rounded-[10px]`. Change the `border-l-[3px]` accent borders from `border-l-purple`, `border-l-green`, `border-l-text-dim` to just using the standard `border-border` on all sides (no colored left border -- Linear aesthetic avoids colored accents on cards). Instead, differentiate cards by their content/labels only.
   - Period selector: Currently uses `rounded-lg border border-border` segmented control. Keep but change active state from `bg-purple-dim text-purple-light` to `bg-bg-card-hover text-text` for a more muted active indicator.
   - Sessions table wrapper: Update `rounded-xl` to `rounded-[10px]`.
   - Active session dot: Keep `bg-green animate-pulse-dot`.
   - Empty sessions: Replace inline empty state with `EmptyState` component. Icon: billing card SVG. Title: "No billing sessions in this period". Description: "Launch an instance to start tracking usage."
   - Retry button on error: change `text-purple hover:text-purple-light` to `text-text-muted hover:text-text` (reduce purple usage).

2. **SSHKeyManager.tsx -- Visual polish**:
   - Key list wrapper: Update `rounded-xl` to `rounded-[10px]`.
   - "Add Key" button: Replace `gradient-btn` with `btn-primary`.
   - Form submit "Add Key" button: Replace `gradient-btn` with `btn-primary`.
   - Form cancel button: use `btn-secondary` styling.
   - Empty state: Replace inline JSX with `EmptyState` component. Icon: key SVG (keep existing). Title: "No SSH keys". Description: "Add an SSH key to connect to your instances."
   - Delete button: Keep red danger styling (not btn-primary).
   - Retry on error: change `text-purple hover:text-purple-light` to `text-text-muted hover:text-text`.
   - Form inputs focus ring: change from `focus:ring-purple/50 focus:border-purple/50` to `focus:ring-1 focus:ring-border-light focus:border-border-light` for subtle, non-purple focus matching Linear aesthetic.

3. **settings/page.tsx -- Visual polish**:
   - Card wrappers: Update `rounded-xl` to `rounded-[10px]`.
   - "Set Limit" / "Update" buttons: Replace `gradient-btn` with `btn-primary`.
   - Remove button: Keep red danger styling.
   - Empty limit state icon container: use `bg-bg-card-hover` instead of `bg-bg-card`.
   - Retry on error: change purple to muted text colors.
   - Input focus rings: change to subtle `focus:ring-1 focus:ring-border-light focus:border-border-light`.
   - Progress bar: change from `bg-purple` to `bg-text-muted` for the under-70% state (reduce purple usage in non-CTA contexts). Keep red and yellow thresholds.

4. **ssh-keys/page.tsx and billing/page.tsx** -- Check if these page files exist and if they use gradient-btn or need updates. Update any page-level heading buttons to use btn-primary. Ensure consistent `type-h3 text-text` for page titles.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npm run build</automated>
  </verify>
  <done>Billing dashboard has muted period selector, no colored left borders on cards, EmptyState for empty sessions. SSH key manager uses btn-primary for add/submit, EmptyState for empty list, subtle focus rings. Settings page uses btn-primary, muted progress bar, subtle focus rings. All pages use consistent 10px border radius and have zero gradient-btn usage.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` succeeds
- `grep -c "gradient-btn" frontend/src/components/cloud/InstancesTable.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/components/cloud/InstanceDetail.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/components/cloud/BillingDashboard.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/components/cloud/SSHKeyManager.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/app/cloud/settings/page.tsx` returns 0
- `grep -c "gradient-btn" frontend/src/app/cloud/instances/page.tsx` returns 0
- `grep -c "colSpan" frontend/src/components/cloud/InstancesTable.tsx` returns 0 (colSpan hack removed)
- `grep -c "EmptyState" frontend/src/components/cloud/InstancesTable.tsx` returns at least 1
- `grep -c "EmptyState" frontend/src/components/cloud/SSHKeyManager.tsx` returns at least 1
- `grep -c "contents" frontend/src/components/cloud/InstancesTable.tsx` returns at least 1 (CSS grid contents)
</verification>

<success_criteria>
1. Instances table columns align with headers -- no colSpan hack, CSS grid layout
2. SSH command column uses available space, not truncated to 260px max-width
3. All dashboard components use btn-primary/btn-secondary instead of gradient-btn
4. Empty states use shared EmptyState component across all pages
5. Billing cards have no colored left borders, muted period selector
6. Input focus rings use subtle border-light color instead of purple
7. All cards use consistent 10px border radius
8. Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-ui-redesign-clean-linear-aesthetic-gpu-catalog-with-categories-and-search-instances-table-fixes-professional-polish/11-03-SUMMARY.md`
</output>
