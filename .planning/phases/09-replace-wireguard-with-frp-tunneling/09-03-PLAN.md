---
phase: 09-replace-wireguard-with-frp-tunneling
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - internal/wireguard/manager.go
  - internal/wireguard/manager_test.go
  - internal/wireguard/ipam.go
  - internal/wireguard/ipam_test.go
  - internal/wireguard/keygen.go
  - internal/wireguard/keygen_test.go
  - internal/wireguard/template.go
  - internal/wireguard/template_test.go
  - internal/wireguard/types.go
  - internal/wireguard/templates/bootstrap.sh.tmpl
  - go.mod
  - go.sum
autonomous: true
requirements:
  - FRP-07

must_haves:
  truths:
    - "internal/wireguard/ directory no longer exists"
    - "wgctrl dependency removed from go.mod"
    - "Project builds and all tests pass without any WireGuard code"
  artifacts:
    - path: "go.mod"
      provides: "Clean dependency list without wgctrl"
  key_links:
    - from: "go.mod"
      to: "go.sum"
      via: "go mod tidy removes unused dependencies"
      pattern: "go mod tidy"
---

<objective>
Delete the entire internal/wireguard/ package and remove the wgctrl dependency from go.mod. Verify the project builds and all tests pass with zero WireGuard references.

Purpose: Complete the migration by removing all dead WireGuard code and dependencies. After this plan, the codebase has a clean, single tunnel implementation using FRP.
Output: Removed wireguard package, cleaned go.mod/go.sum, verified build.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-replace-wireguard-with-frp-tunneling/09-RESEARCH.md
@.planning/phases/09-replace-wireguard-with-frp-tunneling/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete WireGuard package and remove wgctrl dependency</name>
  <files>
    internal/wireguard/manager.go,
    internal/wireguard/manager_test.go,
    internal/wireguard/ipam.go,
    internal/wireguard/ipam_test.go,
    internal/wireguard/keygen.go,
    internal/wireguard/keygen_test.go,
    internal/wireguard/template.go,
    internal/wireguard/template_test.go,
    internal/wireguard/types.go,
    internal/wireguard/templates/bootstrap.sh.tmpl,
    go.mod,
    go.sum
  </files>
  <action>
    1. Delete the entire `internal/wireguard/` directory: `rm -rf internal/wireguard/`.
       Files removed: manager.go, manager_test.go, ipam.go, ipam_test.go, keygen.go, keygen_test.go, template.go, template_test.go, types.go, templates/bootstrap.sh.tmpl.

    2. Verify no remaining imports of `internal/wireguard` anywhere in the codebase:
       `grep -r "internal/wireguard" --include="*.go" .`
       If any remain (from files not updated in plan 02), fix them. Expected: zero matches after plan 02 completed.

    3. Verify no remaining imports of `wgctrl` anywhere:
       `grep -r "wgctrl" --include="*.go" .`
       Expected: zero matches.

    4. Remove the WG config validation block from `internal/config/config.go`:
       - Remove the WG "all three present or all absent" validation block (lines ~101-123 in current file).
       - Remove the WG_ENCRYPTION_KEY decode/validate block (lines ~126-138).
       - Remove the WG fields from the Config struct: WGEncryptionKey, WGEncryptionKeyBytes, WGProxyEndpoint, WGProxyPublicKey, WGInterfaceName.
       - Remove the WG fields from the returned Config literal.
       - Keep the "WireGuard config not set" log line and replace it: remove the else branch that logs about WG.
       - Clean up any unused imports (encoding/hex if only used for WG key decode).

    5. Run `go mod tidy` to remove unused dependencies. This should remove:
       - `golang.zx2c4.com/wireguard/wgctrl` (direct)
       - `golang.zx2c4.com/wireguard` (indirect)
       - `github.com/mdlayher/genetlink` (indirect)
       - `github.com/mdlayher/netlink` (indirect)
       - `github.com/mdlayher/socket` (indirect)
       - `github.com/josharian/native` (indirect)

    6. Run `go build ./...` to verify everything compiles.

    7. Run `go test ./...` to verify all tests pass.

    8. Verify with `go vet ./...` for any remaining issues.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai && go build ./... && go test ./... -count=1 && echo "SUCCESS: build and tests pass without WireGuard"</automated>
  </verify>
  <done>
    internal/wireguard/ directory deleted. wgctrl and 5 indirect WG dependencies removed from go.mod. Config no longer has WG fields. `go build ./...` succeeds. `go test ./...` all pass. `grep -r "wireguard" --include="*.go" .` returns zero matches (except comments if any).
  </done>
</task>

</tasks>

<verification>
- `ls internal/wireguard/ 2>/dev/null` -- directory does not exist
- `grep -r "internal/wireguard" --include="*.go" .` -- zero matches
- `grep "wgctrl" go.mod` -- not present
- `go build ./...` -- compiles
- `go test ./... -count=1` -- all tests pass
- `go vet ./...` -- no issues
</verification>

<success_criteria>
- Zero WireGuard code in the codebase
- wgctrl and related indirect dependencies removed from go.mod
- WG config fields removed from Config struct
- Full build succeeds
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-replace-wireguard-with-frp-tunneling/09-03-SUMMARY.md`
</output>
