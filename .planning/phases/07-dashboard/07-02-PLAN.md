---
phase: 07-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/lib/mock-data.ts
  - frontend/src/components/cloud/StatusBadge.tsx
  - frontend/src/components/cloud/DashboardSidebar.tsx
  - frontend/src/components/cloud/DashboardTopbar.tsx
  - frontend/src/components/cloud/InstancesTable.tsx
  - frontend/src/app/(cloud)/layout.tsx
  - frontend/src/app/(cloud)/page.tsx
  - frontend/src/app/(cloud)/instances/page.tsx
  - frontend/src/app/(cloud)/settings/page.tsx
autonomous: true
requirements: [DASH-05, DASH-08]

must_haves:
  truths:
    - "localhost:3001?site=cloud shows a dashboard with sidebar navigation, topbar, and scrollable content area"
    - "localhost:3001?site=cloud redirects to /instances"
    - "localhost:3001/instances?site=cloud shows a table of GPU instances with status badges and SSH commands"
    - "localhost:3001/settings?site=cloud shows a settings placeholder page"
    - "Sidebar highlights the active page based on current pathname"
    - "Instance status badges show correct colors (running=green, stopped=gray, provisioning=purple, error=red)"
    - "SSH connection command is displayed for running instances"
  artifacts:
    - path: "frontend/src/lib/mock-data.ts"
      provides: "Mock instance data with 5 instances in varied statuses"
      exports: ["MOCK_INSTANCES", "MockInstance"]
    - path: "frontend/src/components/cloud/StatusBadge.tsx"
      provides: "Colored status pill component"
      exports: ["StatusBadge"]
    - path: "frontend/src/components/cloud/DashboardSidebar.tsx"
      provides: "Sidebar navigation with active state"
      exports: ["DashboardSidebar"]
    - path: "frontend/src/components/cloud/DashboardTopbar.tsx"
      provides: "Top bar with breadcrumb and user placeholder"
      exports: ["DashboardTopbar"]
    - path: "frontend/src/components/cloud/InstancesTable.tsx"
      provides: "Desktop table + mobile card view of instances"
      exports: ["InstancesTable"]
    - path: "frontend/src/app/(cloud)/layout.tsx"
      provides: "Dashboard shell with sidebar + topbar + scrollable main"
      exports: ["default", "metadata"]
    - path: "frontend/src/app/(cloud)/instances/page.tsx"
      provides: "Instances list page with mock data"
      exports: ["default"]
  key_links:
    - from: "frontend/src/app/(cloud)/instances/page.tsx"
      to: "frontend/src/lib/mock-data.ts"
      via: "import MOCK_INSTANCES"
      pattern: "MOCK_INSTANCES"
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "frontend/src/components/cloud/StatusBadge.tsx"
      via: "import StatusBadge"
      pattern: "StatusBadge"
    - from: "frontend/src/components/cloud/DashboardSidebar.tsx"
      to: "next/navigation"
      via: "usePathname for active state"
      pattern: "usePathname"
---

<objective>
Build the cloud dashboard shell with sidebar navigation, topbar, instances table (mock data), and settings placeholder.

Purpose: Deliver a working dashboard UI that demonstrates the layout, navigation, and instance management patterns. Uses mock data to build the UI independently of API availability. Satisfies DASH-05 (view/manage instances) and DASH-08 (status + SSH command display) with mock data.

Output: Complete cloud route group with layout shell, 4 cloud components, mock data layer, instances page, settings placeholder.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-dashboard/07-CONTEXT.md
@.planning/phases/07-dashboard/07-RESEARCH.md
@.planning/phases/07-dashboard/07-01-SUMMARY.md

@frontend/src/app/globals.css
@frontend/src/lib/utils.ts
@frontend/src/components/ui/index.ts

<interfaces>
<!-- Design system tokens available from globals.css -->
Colors: bg (#09090f), bg-alt (#0c0c14), bg-card (#111119), bg-card-hover (#16161f)
Text: text (#e2e2ea), text-muted (#8585a0), text-dim (#5c5c78)
Borders: border (#1c1c2a), border-light (#2a2a3d)
Accents: purple (#7c6bf0), green (#22c55e)
Fonts: font-sans (Vremena Grotesk), font-mono (Necto Mono)
Utility: cn() from @/lib/utils

<!-- API response types (for mock data shape) -->
From internal/api/handlers.go:
```typescript
interface InstanceResponse {
  id: string
  name?: string
  status: 'starting' | 'running' | 'stopping' | 'terminated' | 'error'
  gpu_type: string
  gpu_count: number
  tier: 'spot' | 'on_demand'
  region: string
  price_per_hour: number
  connection?: { hostname: string; port: number; ssh_command: string }
  error_reason?: string
  created_at: string
  ready_at?: string
  terminated_at?: string
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock data and cloud UI components</name>
  <files>
    frontend/src/lib/mock-data.ts
    frontend/src/components/cloud/StatusBadge.tsx
    frontend/src/components/cloud/DashboardSidebar.tsx
    frontend/src/components/cloud/DashboardTopbar.tsx
    frontend/src/components/cloud/InstancesTable.tsx
  </files>
  <action>
**Create `frontend/src/lib/mock-data.ts`**:
- Define `MockInstance` type matching the Go backend InstanceResponse shape:
  ```typescript
  export interface MockInstance {
    id: string
    name?: string
    status: 'starting' | 'running' | 'stopping' | 'terminated' | 'error'
    gpu_type: string
    gpu_count: number
    tier: 'spot' | 'on_demand'
    region: string
    price_per_hour: number
    connection?: { hostname: string; port: number; ssh_command: string }
    error_reason?: string
    created_at: string
    ready_at?: string
    terminated_at?: string
  }
  ```
- Export `MOCK_INSTANCES: MockInstance[]` array with 5 instances:
  1. Running H100 SXM x4, spot, us-east, with connection info (ssh command like `ssh root@h100-abc1.gpu.ai -p 10042`)
  2. Running A100 SXM x2, on_demand, eu-west, with connection info
  3. Starting RTX 4090 x1, spot, us-east, no connection (still provisioning)
  4. Error L40S x1, on_demand, ap-south, error_reason: "Provider capacity exhausted"
  5. Terminated A10G x2, spot, us-west, with terminated_at timestamp

**Create `frontend/src/components/cloud/StatusBadge.tsx`**:
- `'use client'` (not strictly needed but keeps it flexible)
- Props: `{ status: MockInstance['status'] }`
- Renders a small pill/badge with:
  - running: green background (green-dim), green text, green dot
  - starting: purple background (purple-dim), purple text, animated pulse dot
  - stopping: yellow-ish background, muted text
  - terminated: dark/gray background, dim text
  - error: red background tint, red text
- Use `cn()` for conditional classes
- Use the design system colors from globals.css (not hardcoded hex)
- Font: type-ui-xs (12px)

**Create `frontend/src/components/cloud/DashboardSidebar.tsx`**:
- `'use client'` (uses usePathname)
- Import `usePathname` from `next/navigation`
- Import `Link` from `next/link`
- Nav items: Instances (/instances), GPU Availability (/gpu-availability), SSH Keys (/ssh-keys), Billing (/billing), Settings (/settings)
- Each nav item has an icon (use simple SVG inline or text symbol -- no icon library needed), label, and href
- Active state: highlight the item matching current pathname with bg-card-hover background and text color, inactive items use text-muted
- GPU.ai logo at top (text-based: "GPU.ai" in font-sans, link to "/" to go back to marketing)
- Sidebar width: w-60 (240px) on desktop
- On mobile (below md breakpoint): hide sidebar entirely (hidden md:flex) -- the topbar can have a menu trigger later
- Background: bg-alt, right border with border color
- All nav link hrefs MUST include `?site=cloud` suffix to maintain cloud routing in local dev. Use a helper or build hrefs as `${path}?site=cloud` for local dev compatibility, OR rely on the proxy.ts cookie/session (simpler: just use plain paths since the proxy rewrites work per-request). Use plain paths -- the proxy handles it.

**IMPORTANT for sidebar links:** Since proxy.ts reads `?site=cloud` from the request URL, once you're in the cloud context via `?site=cloud`, clicking plain `/instances` will lose the query param and route to marketing. To handle this, the DashboardSidebar should preserve the `?site=cloud` param. Use `useSearchParams` to detect if `site=cloud` is present, and if so, append it to all nav hrefs. This ensures local dev works correctly.

**Create `frontend/src/components/cloud/DashboardTopbar.tsx`**:
- `'use client'`
- Horizontal bar at top of content area
- Left side: breadcrumb-style text showing current page name (derive from pathname, e.g., `/instances` → "Instances", `/settings` → "Settings")
- Right side: hardcoded user avatar placeholder (initials circle like "AR") + "Sign Out" text button (non-functional -- Clerk replaces in Plan 03)
- Background: bg, bottom border with border color
- Height: h-14 (56px)
- Use `usePathname` from `next/navigation` to derive breadcrumb text

**Create `frontend/src/components/cloud/InstancesTable.tsx`**:
- `'use client'`
- Props: `{ instances: MockInstance[] }`
- **Desktop view (md+):** HTML table with columns:
  - Name/ID (show name if present, fallback to truncated ID)
  - GPU (type x count)
  - Status (StatusBadge component)
  - Region
  - Tier (formatted: "Spot" or "On-Demand")
  - Cost ($/hr formatted)
  - SSH Command (show command if connection exists, otherwise "--")
  - The SSH command column should have a copy button (clipboard icon, uses navigator.clipboard.writeText)
- **Mobile view (below md):** Stack of cards, each showing instance info in a compact card format with StatusBadge
- Use design system classes: bg-card for rows/cards, border for dividers, text-muted for labels, font-mono for SSH commands
- Empty state: "No instances" message if array is empty
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && test -f src/lib/mock-data.ts && test -f src/components/cloud/StatusBadge.tsx && test -f src/components/cloud/DashboardSidebar.tsx && test -f src/components/cloud/DashboardTopbar.tsx && test -f src/components/cloud/InstancesTable.tsx && echo "All cloud components exist"</automated>
  </verify>
  <done>Mock data file with 5 instances and all 4 cloud components (StatusBadge, DashboardSidebar, DashboardTopbar, InstancesTable) created with proper styling using design system tokens</done>
</task>

<task type="auto">
  <name>Task 2: Create cloud route group pages and verify build</name>
  <files>
    frontend/src/app/(cloud)/layout.tsx
    frontend/src/app/(cloud)/page.tsx
    frontend/src/app/(cloud)/instances/page.tsx
    frontend/src/app/(cloud)/settings/page.tsx
  </files>
  <action>
**Create `frontend/src/app/(cloud)/layout.tsx`**:
- Export metadata: `{ title: "Dashboard" }` (will render as "Dashboard | GPU.ai" via root template)
- Layout structure:
  ```tsx
  <div className="flex h-screen">
    <DashboardSidebar />
    <div className="flex flex-1 flex-col overflow-hidden">
      <DashboardTopbar />
      <main className="flex-1 overflow-y-auto p-6">
        {children}
      </main>
    </div>
  </div>
  ```
- The main area uses bg (same as body) and has padding for content
- Import DashboardSidebar and DashboardTopbar from @/components/cloud/

**Create `frontend/src/app/(cloud)/page.tsx`**:
- Import `redirect` from `next/navigation`
- Call `redirect("/instances")` -- this page just redirects to the instances list
- This is a Server Component (no 'use client')

**Create `frontend/src/app/(cloud)/instances/page.tsx`**:
- Export metadata: `{ title: "Instances" }`
- Import `MOCK_INSTANCES` from `@/lib/mock-data`
- Import `InstancesTable` from `@/components/cloud/InstancesTable`
- Render a page with:
  - Header row: "Instances" title (type-h2 or type-h3) + "Launch Instance" button (styled with gradient-btn class, non-functional for now)
  - `<InstancesTable instances={MOCK_INSTANCES} />`
- This can be a Server Component since mock data is static -- the InstancesTable itself is 'use client'

**Create `frontend/src/app/(cloud)/settings/page.tsx`**:
- Export metadata: `{ title: "Settings" }`
- Render a simple placeholder: "Settings" heading + "Settings page coming soon." text in text-muted
- Server Component

**Verify build:**
Run `npx tsc --noEmit` to confirm no TypeScript errors across the entire project.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Cloud route group has layout with sidebar+topbar shell, root redirect to /instances, instances page with mock data table showing status badges and SSH commands, settings placeholder, all compiling without TypeScript errors</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with no errors
2. All cloud component files exist in `src/components/cloud/`
3. Mock data file exports `MOCK_INSTANCES` with 5 instances
4. `(cloud)/page.tsx` contains redirect to `/instances`
5. `(cloud)/instances/page.tsx` renders InstancesTable with MOCK_INSTANCES
6. `(cloud)/settings/page.tsx` renders placeholder
7. DashboardSidebar has nav items for: Instances, GPU Availability, SSH Keys, Billing, Settings
8. InstancesTable shows SSH command for running instances (DASH-08)
</verification>

<success_criteria>
- Dashboard shell renders with sidebar, topbar, and scrollable content area
- Sidebar highlights active page based on pathname
- Instances page shows table with 5 mock instances in varied statuses
- StatusBadge colors match status (running=green, starting=purple, error=red, etc.)
- SSH connection command visible for running instances
- Settings page shows placeholder content
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-dashboard/07-02-SUMMARY.md`
</output>
