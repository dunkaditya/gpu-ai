---
phase: 08-rebuild-frontend-landing-page-to-match-vercel-homepage-design
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - frontend/src/components/landing/Navbar.tsx
  - frontend/src/components/landing/Hero.tsx
  - frontend/src/components/landing/UseCaseTabs.tsx
  - frontend/src/components/landing/FeaturePillars.tsx
  - frontend/src/components/landing/CLIDemo.tsx
  - frontend/src/components/landing/FinalCTA.tsx
  - frontend/src/components/landing/Footer.tsx
  - frontend/src/app/page.tsx
autonomous: true
requirements:
  - DASH-01

must_haves:
  truths:
    - "Landing page renders all 7 sections in correct order: Navbar, Hero, UseCaseTabs, FeaturePillars, CLIDemo, FinalCTA, Footer"
    - "Navbar shows GPU.ai text wordmark with navigation links and CTAs, fixed to top with glassmorphism on scroll"
    - "Hero section displays centered headline, subtitle, two CTA buttons ('Launch GPU Instance' + 'Talk to our team'), static metrics row, and triangle SVG with rainbow glow"
    - "Use Case Tabs section has 5 clickable tabs (ML Training, Inference, Fine-tuning, Rendering, Research) with content switching"
    - "Feature Pillars section shows 3 sections: Source, Deploy, Scale with descriptions and feature lists"
    - "CLI Demo section shows a styled terminal window with gpuctl command examples"
    - "Footer has multi-column link grid with GPU.ai branding"
    - "Page uses dark theme throughout with Vercel-style grid background visible behind all sections"
  artifacts:
    - path: "frontend/src/components/landing/Navbar.tsx"
      provides: "Fixed navigation bar with GPU.ai wordmark, nav links, Log in + Sign Up CTAs, mobile hamburger"
      min_lines: 40
    - path: "frontend/src/components/landing/Hero.tsx"
      provides: "Hero section with headline, subtitle, CTAs, metrics row, triangle graphic with rainbow glow"
      min_lines: 50
    - path: "frontend/src/components/landing/UseCaseTabs.tsx"
      provides: "Client component with 5 horizontal tabs switching content panels"
      min_lines: 40
    - path: "frontend/src/components/landing/FeaturePillars.tsx"
      provides: "Three feature sections: Source, Deploy, Scale"
      min_lines: 40
    - path: "frontend/src/components/landing/CLIDemo.tsx"
      provides: "Terminal window with gpuctl command examples and syntax coloring"
      min_lines: 30
    - path: "frontend/src/components/landing/FinalCTA.tsx"
      provides: "Final call-to-action section with two buttons"
      min_lines: 15
    - path: "frontend/src/components/landing/Footer.tsx"
      provides: "Multi-column footer with links and branding"
      min_lines: 30
    - path: "frontend/src/app/page.tsx"
      provides: "Page composition importing and rendering all 7 sections"
      min_lines: 20
  key_links:
    - from: "frontend/src/app/page.tsx"
      to: "frontend/src/components/landing/*.tsx"
      via: "imports all 7 section components"
      pattern: "import.*components/landing"
    - from: "frontend/src/components/landing/UseCaseTabs.tsx"
      to: "frontend/src/lib/constants.ts"
      via: "imports USE_CASE_TABS data"
      pattern: "USE_CASE_TABS"
    - from: "frontend/src/components/landing/Hero.tsx"
      to: "frontend/src/lib/constants.ts"
      via: "imports HERO_CONTENT data"
      pattern: "HERO_CONTENT"
    - from: "frontend/src/components/landing/Navbar.tsx"
      to: "frontend/src/lib/constants.ts"
      via: "imports NAV_LINKS data"
      pattern: "NAV_LINKS"
---

<objective>
Build all 7 landing page sections matching Vercel's homepage layout and compose them into the final page.

Purpose: This plan creates every visible section of the rebuilt landing page. The foundation (fonts, design system, constants, deleted old components) was established in Plan 01. This plan builds the actual UI: Navbar with glassmorphism, Hero with triangle + rainbow glow, tabbed use cases, feature pillars, CLI terminal demo, final CTA, and multi-column footer.

Output: A complete, building landing page at `/` that mirrors Vercel's homepage structure with GPU.ai content, dark theme throughout, grid background visible behind all sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rebuild-frontend-landing-page-to-match-vercel-homepage-design/08-CONTEXT.md
@.planning/phases/08-rebuild-frontend-landing-page-to-match-vercel-homepage-design/08-RESEARCH.md
@.planning/phases/08-rebuild-frontend-landing-page-to-match-vercel-homepage-design/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that this plan depends on. -->

From frontend/src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From frontend/src/lib/constants.ts:
```typescript
export const NAV_LINKS: Array<{ label: string; href: string }>;
export const HERO_CONTENT: {
  headline: string;
  subtitle: string;
  primaryCTA: string;
  secondaryCTA: string;
  metrics: Array<{ value: string; label: string }>;
};
export const USE_CASE_TABS: Array<{
  id: string;
  label: string;
  title: string;
  description: string;
  features: string[];
}>;
export const FEATURE_PILLARS: Array<{
  id: string;
  title: string;
  subtitle: string;
  description: string;
  features: string[];
}>;
export const CLI_COMMANDS: Array<{
  prompt: string;
  command: string;
  output: string[];
}>;
export const FOOTER_COLUMNS: Array<{
  title: string;
  links: Array<{ label: string; href: string }>;
}>;
```

From frontend/src/components/ui/Button.tsx:
```typescript
export function Button({ variant, size, className, children, ...props }: ButtonProps): JSX.Element;
// variant: "primary" | "secondary" (default "primary")
// size: "sm" | "md" | "lg" (default "md")
```

From frontend/src/components/ui/Container.tsx:
```typescript
export function Container({ className, children, ...props }: HTMLAttributes<HTMLDivElement>): JSX.Element;
// Max-width 1200px with px-6 horizontal padding
```

From frontend/src/app/globals.css (CSS classes available):
- `.grid-background` — Full-page background with grid lines and cross markers
- `.rainbow-glow` — Radial gradient with blue/green/red/purple colors, blurred 60px
- `.glass-nav` — Backdrop blur + dark semi-transparent background for fixed navbar
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Navbar, Hero, and UseCaseTabs components</name>
  <files>
    frontend/src/components/landing/Navbar.tsx
    frontend/src/components/landing/Hero.tsx
    frontend/src/components/landing/UseCaseTabs.tsx
  </files>
  <action>
**Component 1: `frontend/src/components/landing/Navbar.tsx`** — Client component ("use client")

Fixed navbar matching Vercel's design:
- `<nav>` with `fixed top-0 z-50 w-full` positioning
- Border bottom: `border-b border-white/[0.08]`
- Apply `glass-nav` class for backdrop blur effect
- Inner container: `mx-auto flex h-16 max-w-[1200px] items-center justify-between px-6`
- Left: GPU.ai text wordmark — `<span>` or `<a href="/">` with `text-lg font-semibold tracking-tight text-white`
- Center (hidden on mobile, visible md+): Map over `NAV_LINKS` from constants.ts, render as `<a>` links with `text-sm text-gray-400 hover:text-white transition-colors`
- Right: Two elements:
  - "Log in" link: `text-sm text-gray-400 hover:text-white transition-colors`
  - "Sign Up" button: Use `<Button variant="primary" size="sm">Sign Up</Button>`
- Mobile hamburger: Hidden on md+, visible on mobile. Use a simple 3-line SVG icon button. On click, toggle a mobile menu dropdown that shows the nav links vertically.
- Use `useState` for mobile menu toggle and `useEffect` + scroll listener to add/remove border visibility (Vercel-style: border becomes more visible after scrolling past 0).
- Import `NAV_LINKS` from `@/lib/constants`, `Button` from `@/components/ui`, `cn` from `@/lib/utils`.

**Component 2: `frontend/src/components/landing/Hero.tsx`** — Server component (no "use client")

Centered hero section matching Vercel's layout:
- Outer wrapper: `<section>` with `relative pt-32 pb-24 overflow-visible` (pt-32 accounts for fixed nav height)
- Container (max-width 1200px, centered)
- Content wrapper: `text-center mx-auto max-w-[800px]`
- Headline: `<h1>` with `text-5xl md:text-7xl font-bold tracking-tight text-white leading-[1.1]` — import from `HERO_CONTENT.headline`
- Subtitle: `<p>` with `mt-6 text-lg md:text-xl text-text-muted max-w-[600px] mx-auto` — import from `HERO_CONTENT.subtitle`
- CTA row: `flex items-center justify-center gap-4 mt-10`
  - Primary: `<Button variant="primary" size="lg" href="#">{HERO_CONTENT.primaryCTA}</Button>`
  - Secondary: `<Button variant="secondary" size="lg" href="#">{HERO_CONTENT.secondaryCTA}</Button>`
- Metrics row: `flex items-center justify-center gap-8 md:gap-12 mt-16` — Map over `HERO_CONTENT.metrics`, each rendered as:
  - `<div className="text-center">`
  - Value: `<div className="text-2xl md:text-3xl font-bold text-white">{metric.value}</div>`
  - Label: `<div className="mt-1 text-sm text-text-muted">{metric.label}</div>`

- Triangle + rainbow glow section (below metrics): `relative mt-20 flex justify-center`
  - Rainbow glow div: `absolute inset-0 flex justify-center` containing a `<div className="rainbow-glow w-[500px] h-[300px] md:w-[700px] md:h-[400px]" />`
  - Triangle SVG on top: `relative z-10` — white filled triangle, roughly `w-[120px] h-[120px] md:w-[180px] md:h-[180px]`
  - SVG viewBox `0 0 1024 1024`, path `M512 128L896 832H128L512 128Z`, fill white

**Component 3: `frontend/src/components/landing/UseCaseTabs.tsx`** — Client component ("use client")

Horizontal tab section matching Vercel's category tabs:
- Outer `<section>` with `py-24`
- Container (max-width)
- Section heading (optional): could add "Built for every GPU workload" or similar in `text-3xl font-bold text-white text-center mb-12`
- Tab row: `flex border-b border-white/10 justify-center overflow-x-auto`
  - Map over `USE_CASE_TABS`, each tab is a `<button>`:
  - Active: `border-b-2 border-white text-white`
  - Inactive: `text-gray-400 hover:text-gray-200`
  - All: `px-5 py-3 text-sm font-medium transition-colors whitespace-nowrap`
- Content panel: `mt-12`
  - Active tab's content displayed in a card-like layout:
  - Title: `text-2xl md:text-3xl font-bold text-white`
  - Description: `mt-4 text-text-muted text-lg max-w-[700px]`
  - Features: `mt-8 grid grid-cols-1 md:grid-cols-2 gap-4`
    - Each feature: `flex items-start gap-3`
      - Check icon (small SVG checkmark or bullet): `text-green-400 mt-1 shrink-0`
      - Text: `text-sm text-text-muted`
- Use `useState` for active tab index. Default to 0.
- Import `USE_CASE_TABS` from `@/lib/constants`, `Container` from `@/components/ui`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Navbar.tsx renders fixed nav with GPU.ai wordmark, nav links, Login/SignUp buttons, mobile hamburger
    - Hero.tsx renders centered headline, subtitle, two CTAs, metrics row, triangle with rainbow glow
    - UseCaseTabs.tsx renders 5 clickable tabs that switch content panels
    - All three components type-check cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Build FeaturePillars, CLIDemo, FinalCTA, Footer, and compose page.tsx</name>
  <files>
    frontend/src/components/landing/FeaturePillars.tsx
    frontend/src/components/landing/CLIDemo.tsx
    frontend/src/components/landing/FinalCTA.tsx
    frontend/src/components/landing/Footer.tsx
    frontend/src/app/page.tsx
  </files>
  <action>
**Component 4: `frontend/src/components/landing/FeaturePillars.tsx`** — Server component

Three feature sections matching Vercel's "Your Product, Delivered" layout:
- Outer `<section>` with `py-24`
- Container (max-width)
- Map over `FEATURE_PILLARS` (3 items: Source, Deploy, Scale). Each pillar:
  - Wrapper: `py-16 border-b border-white/[0.08] last:border-b-0` (horizontal dividers between pillars)
  - Layout: `grid grid-cols-1 md:grid-cols-2 gap-12 items-start`
  - Left column (text):
    - Pillar title: `text-sm font-medium uppercase tracking-widest text-text-dim` (e.g., "Source", "Deploy", "Scale")
    - Subtitle: `mt-4 text-3xl md:text-4xl font-bold text-white leading-tight`
    - Description: `mt-4 text-text-muted text-lg leading-relaxed`
  - Right column (features):
    - Feature list: `space-y-4`
    - Each feature: `flex items-start gap-3`
      - Bullet: a small `<div>` circle (`w-2 h-2 rounded-full bg-white/20 mt-2 shrink-0`) or a check icon
      - Text: `text-text-muted`

**Component 5: `frontend/src/components/landing/CLIDemo.tsx`** — Server component

Terminal window matching Vercel's AI Gateway code block:
- Outer `<section>` with `py-24`
- Container (max-width)
- Section heading: `text-center mb-12`
  - Title: `text-3xl md:text-4xl font-bold text-white` — "Powerful CLI, zero friction" or similar
  - Subtitle: `mt-4 text-text-muted text-lg` — brief description of gpuctl
- Terminal window: `mx-auto max-w-[800px]`
  - `rounded-xl border border-white/10 bg-[#0a0a0a] overflow-hidden`
  - Chrome bar: `flex items-center gap-2 border-b border-white/10 px-4 py-3`
    - Three dots: `flex gap-1.5` with `<span className="h-3 w-3 rounded-full bg-[#ff5f56]" />` (red), `bg-[#ffbd2e]` (yellow), `bg-[#27c93f]` (green)
    - Terminal label: `ml-2 text-xs text-gray-500` — "terminal"
  - Code area: `p-6 font-mono text-sm space-y-4`
  - Map over `CLI_COMMANDS`, for each command:
    - `<div>` with command line:
      - Prompt: `<span className="text-green-400">$</span>`
      - Space
      - Command: `<span className="text-white">{cmd.command}</span>`
    - Output lines: each `<div className="text-text-muted pl-4">{line}</div>`

**Component 6: `frontend/src/components/landing/FinalCTA.tsx`** — Server component

Simple call-to-action section:
- Outer `<section>` with `py-24`
- Container, centered text: `text-center`
- Headline: `text-3xl md:text-5xl font-bold text-white` — "Ready to deploy?" or "Start building with GPU.ai"
- Subtitle: `mt-6 text-text-muted text-lg max-w-[500px] mx-auto` — brief encouraging line
- CTA buttons: `flex items-center justify-center gap-4 mt-10`
  - `<Button variant="primary" size="lg" href="#">Launch GPU Instance</Button>`
  - `<Button variant="secondary" size="lg" href="#">Talk to our team</Button>`

**Component 7: `frontend/src/components/landing/Footer.tsx`** — Server component

Multi-column footer matching Vercel's footer:
- Outer `<footer>` with `border-t border-white/[0.08] py-16`
- Container (max-width)
- Top section: `grid grid-cols-2 md:grid-cols-5 gap-8`
  - First column (spans 1 col): GPU.ai branding
    - Logo: `<span className="text-lg font-semibold text-white">GPU.ai</span>`
    - Tagline: `mt-2 text-sm text-text-dim` — "GPU compute, simplified."
  - Remaining 4 columns: Map over `FOOTER_COLUMNS`
    - Column title: `text-sm font-medium text-white`
    - Links: `mt-4 space-y-3`
      - Each: `<a className="block text-sm text-text-muted hover:text-white transition-colors">`
- Bottom section: `mt-12 flex items-center justify-between border-t border-white/[0.08] pt-8`
  - Left: `<span className="text-sm text-text-dim">&copy; 2026 GPU.ai. All rights reserved.</span>`
  - Right (optional): social links or empty

**Compose page.tsx: `frontend/src/app/page.tsx`**

Replace placeholder with full page composition:
```typescript
import { Navbar } from "@/components/landing/Navbar";
import { Hero } from "@/components/landing/Hero";
import { UseCaseTabs } from "@/components/landing/UseCaseTabs";
import { FeaturePillars } from "@/components/landing/FeaturePillars";
import { CLIDemo } from "@/components/landing/CLIDemo";
import { FinalCTA } from "@/components/landing/FinalCTA";
import { Footer } from "@/components/landing/Footer";

export default function LandingPage() {
  return (
    <div className="grid-background">
      <Navbar />
      <main>
        <Hero />
        <UseCaseTabs />
        <FeaturePillars />
        <CLIDemo />
        <FinalCTA />
      </main>
      <Footer />
    </div>
  );
}
```

Ensure all content is rendered as Server Components except Navbar (mobile menu + scroll) and UseCaseTabs (tab switching) which are "use client". All other components (Hero, FeaturePillars, CLIDemo, FinalCTA, Footer) must NOT have "use client".
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 7 section components exist and are correctly typed
    - page.tsx imports and renders all 7 sections in order: Navbar, Hero, UseCaseTabs, FeaturePillars, CLIDemo, FinalCTA, Footer
    - `npm run build` passes cleanly
    - Only Navbar.tsx and UseCaseTabs.tsx use "use client" directive
    - Page uses grid-background wrapper for continuous grid pattern
    - Hero has triangle SVG with rainbow-glow effect
    - Dark theme throughout with Vercel-style typography and spacing
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` completes without errors
2. `grep -c "use client" frontend/src/components/landing/*.tsx` shows exactly 2 (Navbar, UseCaseTabs)
3. `grep "grid-background" frontend/src/app/page.tsx` returns a match
4. All 7 component files exist: `ls frontend/src/components/landing/{Navbar,Hero,UseCaseTabs,FeaturePillars,CLIDemo,FinalCTA,Footer}.tsx`
5. page.tsx imports all 7 components: `grep -c "import" frontend/src/app/page.tsx` returns 7
6. No old component imports remain in page.tsx: `grep -c "TrustBar\|PricingTable\|HowItWorks\|Features\|EffectsToggle\|ComputeField" frontend/src/app/page.tsx` returns 0
</verification>

<success_criteria>
- Complete landing page builds and renders all 7 sections
- Section order matches CONTEXT.md specification: Navbar, Hero, UseCaseTabs, FeaturePillars, CLIDemo, FinalCTA, Footer
- Hero has "Launch GPU Instance" and "Talk to our team" CTAs per user decision
- Use Case tabs show ML Training, Inference, Fine-tuning, Rendering, Research per user decision
- Feature Pillars show Source, Deploy, Scale per user decision
- CLI Demo shows gpuctl command examples per user decision
- Dark theme throughout per user decision
- Grid background with cross markers visible per user decision
- Triangle SVG with rainbow glow in hero per user decision
- Only 2 client components (Navbar, UseCaseTabs) — rest are server components
</success_criteria>

<output>
After completion, create `.planning/phases/08-rebuild-frontend-landing-page-to-match-vercel-homepage-design/08-02-SUMMARY.md`
</output>
