---
phase: 04.1-wireguard-integration-wiring
plan: 02
subsystem: infra
tags: [wireguard, provisioning, runpod, addpeer, dockerargs, rollback]

# Dependency graph
requires:
  - phase: 04.1-wireguard-integration-wiring
    provides: "Optional WG config, Manager/IPAM init wiring in main.go"
  - phase: 04-auth-instance-lifecycle
    provides: "Provisioning engine with nil-guard WG pattern, RunPod adapter"
  - phase: 03-privacy-layer
    provides: "WireGuard Manager.AddPeer/RemovePeer, IPAM, cloud-init bootstrap"
provides:
  - "AddPeer called in provisioning flow before provider.Provision (peer ready before instance boots)"
  - "Best-effort RemovePeer rollback if provider.Provision fails"
  - "RunPod adapter delivers StartupScript as dockerArgs in both on-demand and spot paths"
  - "Complete end-to-end WG wiring: keys -> IPAM -> peer -> cloud-init -> dockerArgs delivery"
affects: [provisioning, wireguard, runpod]

# Tech tracking
tech-stack:
  added: []
  patterns: ["pre-provision peer registration with rollback cleanup", "conditional dockerArgs delivery for backward compatibility"]

key-files:
  created: []
  modified:
    - internal/provision/engine.go
    - internal/provider/runpod/adapter.go

key-decisions:
  - "AddPeer placed after IPAM allocation but before provider.Provision -- peer must exist on proxy before instance boots"
  - "RemovePeer rollback uses wgPubKey (outer scope) instead of kp.PublicKey (inner scope) for correct variable scoping"
  - "dockerArgs only set when startupScript is non-empty -- maintains backward compatibility for tests without WG"

patterns-established:
  - "Pre-provision resource registration with best-effort rollback on downstream failure"

requirements-completed: [PRIV-02, PRIV-05, PRIV-06]

# Metrics
duration: 2min
completed: 2026-02-25
---

# Phase 04.1 Plan 02: WireGuard Provisioning Flow Wiring Summary

**AddPeer registered before provider.Provision with rollback cleanup, StartupScript delivered as dockerArgs to RunPod pods**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-25T06:47:55Z
- **Completed:** 2026-02-25T06:49:41Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- WireGuard AddPeer call wired into provisioning flow after IPAM allocation, before provider.Provision
- Best-effort RemovePeer cleanup runs if provider.Provision fails (matches Terminate's cleanup pattern)
- RunPod adapter delivers StartupScript as dockerArgs in both on-demand and spot provision paths
- Full end-to-end WG privacy layer wiring now complete across all components

## Task Commits

Each task was committed atomically:

1. **Task 1: Add AddPeer call in engine.Provision with rollback on failure** - `d773b2c` (feat)
2. **Task 2: Pass StartupScript as dockerArgs in RunPod adapter** - `3bd72ba` (feat)

## Files Created/Modified
- `internal/provision/engine.go` - AddPeer call after IPAM allocation; RemovePeer rollback in provider.Provision error path
- `internal/provider/runpod/adapter.go` - startupScript parameter added to provisionOnDemand and provisionSpot; dockerArgs set conditionally

## Decisions Made
- AddPeer placed after IPAM allocation but before provider.Provision: peer must exist on the proxy before the instance boots so the WireGuard tunnel handshake succeeds
- RemovePeer rollback uses `wgPubKey` (declared in outer scope as `*string`) rather than `kp.PublicKey` (scoped inside WG init block) for correct Go variable scoping
- dockerArgs only set when startupScript is non-empty to maintain backward compatibility: existing tests create pods without WG scripts and should not be affected

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Full WireGuard privacy layer wiring is complete: config -> main.go init -> engine AddPeer -> cloud-init render -> RunPod dockerArgs delivery
- The privacy layer is now functionally connected end-to-end across all 4 files (config.go, main.go, engine.go, adapter.go)
- Remaining gap closure phases (04.2 billing, 04.3 health monitoring) can proceed independently

---
*Phase: 04.1-wireguard-integration-wiring*
*Completed: 2026-02-25*

## Self-Check: PASSED
- All files exist (engine.go, adapter.go, SUMMARY.md)
- All commits verified (d773b2c, 3bd72ba)
- Build passes, vet clean, all tests pass
