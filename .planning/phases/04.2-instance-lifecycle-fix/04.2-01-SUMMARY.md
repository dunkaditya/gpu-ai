---
phase: 04.2-instance-lifecycle-fix
plan: 01
subsystem: api
tags: [middleware, auth, bearer-token, instance-callback, config]

# Dependency graph
requires:
  - phase: 04-auth-instance-lifecycle
    provides: "InternalAuthMiddleware, LocalhostOnly, callback handlers, db.GetInstance"
provides:
  - "InstanceTokenAuth middleware for per-instance Bearer token validation"
  - "GpuctlPublicURL config field for callback URL construction"
  - "Callback routes reachable from remote GPU instances"
affects: [04.2-instance-lifecycle-fix, cloud-init, provisioning]

# Tech tracking
tech-stack:
  added: []
  patterns: ["per-instance token auth replacing global shared secret + localhost restriction"]

key-files:
  created: []
  modified:
    - internal/api/middleware.go
    - internal/api/middleware_test.go
    - internal/api/server.go
    - internal/api/handlers_internal.go
    - internal/config/config.go

key-decisions:
  - "InstanceTokenAuth validates per-instance tokens via DB lookup, replacing LocalhostOnly + InternalAuthMiddleware on callback routes"
  - "GpuctlPublicURL is optional config -- empty falls back to branded hostname for dev"
  - "Redundant per-handler token checks removed -- middleware handles all auth"

patterns-established:
  - "Per-instance auth: InstanceTokenAuth middleware for routes that GPU instances call back to"

requirements-completed: [INST-07]

# Metrics
duration: 2min
completed: 2026-02-25
---

# Phase 04.2 Plan 01: Callback Auth Fix Summary

**InstanceTokenAuth middleware replacing LocalhostOnly on callback routes, with GpuctlPublicURL config for reachable callback URLs**

## Performance

- **Duration:** 2 min
- **Started:** 2026-02-25T07:22:16Z
- **Completed:** 2026-02-25T07:24:41Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- InstanceTokenAuth middleware validates per-instance Bearer tokens against the database
- Callback routes (`/internal/instances/{id}/ready` and `/health`) now reachable from remote GPU instances
- GpuctlPublicURL config field available for constructing callback URLs in Plan 02
- Redundant per-handler token checks removed from both callback handlers

## Task Commits

Each task was committed atomically:

1. **Task 1: Add GPUCTL_PUBLIC_URL config and InstanceTokenAuth middleware** - `5445cc2` (feat)
2. **Task 2: Replace LocalhostOnly on callback routes with InstanceTokenAuth** - `2889a28` (feat)

## Files Created/Modified
- `internal/api/middleware.go` - Added InstanceTokenAuth middleware with DB-backed per-instance token validation
- `internal/api/middleware_test.go` - Added 3 tests for header parsing edge cases
- `internal/api/server.go` - Callback routes now use InstanceTokenAuth instead of LocalhostOnly+InternalAuthMiddleware
- `internal/api/handlers_internal.go` - Removed redundant token checks from handleInstanceReady and handleInstanceHealth
- `internal/config/config.go` - Added GpuctlPublicURL field loaded from GPUCTL_PUBLIC_URL env var

## Decisions Made
- InstanceTokenAuth validates per-instance tokens via DB lookup, replacing LocalhostOnly + InternalAuthMiddleware on callback routes
- GpuctlPublicURL is optional config -- empty falls back to branded hostname for dev
- Redundant per-handler token checks removed -- middleware handles all auth before handler runs

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- InstanceTokenAuth middleware available for any future internal callback routes
- GpuctlPublicURL config field ready for Plan 02 to use when constructing callback URLs in cloud-init/startup scripts
- Plan 02 can now wire the callback URL using GpuctlPublicURL instead of unreachable branded hostname

---
*Phase: 04.2-instance-lifecycle-fix*
*Completed: 2026-02-25*
