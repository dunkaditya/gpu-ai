---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - docker-compose.yml
  - .env.example
  - go.mod
  - go.sum
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Config loads DATABASE_URL, REDIS_URL, INTERNAL_API_TOKEN as required env vars and fails startup with clear error listing all missing vars"
    - "Config loads GPUCTL_PORT with default 9090 when not set"
    - "Config rejects startup if INTERNAL_API_TOKEN is set to 'change-me'"
    - "docker compose up -d starts Postgres and Redis with connection strings matching .env.example"
  artifacts:
    - path: "internal/config/config.go"
      provides: "Typed Config struct with Load() function"
      exports: ["Config", "Load"]
      min_lines: 50
    - path: "docker-compose.yml"
      provides: "Local dev Postgres and Redis services"
      contains: "postgres"
    - path: "go.mod"
      provides: "Go module with pgx and go-redis dependencies"
      contains: "pgx/v5"
  key_links:
    - from: "internal/config/config.go"
      to: "os.Getenv / os.LookupEnv"
      via: "stdlib env var reading"
      pattern: "os\\.Getenv|os\\.LookupEnv"
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "matching connection strings"
      pattern: "gpuai:gpuai@localhost:5432"
---

<objective>
Implement environment configuration loading and local development infrastructure.

Purpose: Config is the root dependency for everything else in Phase 1 -- database pool, Redis client, API server, and health endpoint all need configuration values. Docker Compose provides the local Postgres and Redis that the binary connects to.

Output: Working `config.Load()` function, `docker-compose.yml` for local dev, Go module with required dependencies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@internal/config/config.go
@.env.example
@go.mod
@cmd/gpuctl/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement config loading with validation</name>
  <files>internal/config/config.go</files>
  <action>
Replace the TODO stub in `internal/config/config.go` with a working implementation:

1. Define `Config` struct with these fields (Phase 1 scope only):
   - `Port` string — from `GPUCTL_PORT`, default `"9090"`
   - `DatabaseURL` string — from `DATABASE_URL`, required
   - `RedisURL` string — from `REDIS_URL`, required
   - `InternalAPIToken` string — from `INTERNAL_API_TOKEN`, required

2. Implement `func Load() (*Config, error)`:
   - Use `os.Getenv()` for required vars, `os.LookupEnv()` or helper for optional vars with defaults
   - Collect ALL missing required vars into a slice, then return a single error listing them all (not fail-fast on the first missing var)
   - Error format: `"missing required environment variables: DATABASE_URL, REDIS_URL"`
   - After validation, check if `INTERNAL_API_TOKEN == "change-me"` and return error: `"INTERNAL_API_TOKEN must be changed from default value 'change-me'"`
   - Return `*Config` on success

3. Add a helper function `getEnvDefault(key, defaultVal string) string` for optional vars.

4. Package doc comment: `// Package config loads gpuctl configuration from environment variables.`

Follow conventions: no globals, no init(), no auto-loading of .env files. Use stdlib only (os, fmt, strings).
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai && go vet ./internal/config/</automated>
    <manual>Review that Load() collects all missing vars before returning error</manual>
  </verify>
  <done>Config struct defined with 4 fields, Load() validates required vars and rejects default token value, go vet passes</done>
</task>

<task type="auto">
  <name>Task 2: Create Docker Compose and initialize Go dependencies</name>
  <files>docker-compose.yml, .env.example, go.mod, go.sum</files>
  <action>
1. Create `docker-compose.yml` at project root with:
   - `postgres` service: image `postgres:16`, env vars `POSTGRES_USER=gpuai`, `POSTGRES_PASSWORD=gpuai`, `POSTGRES_DB=gpuai`, port `5432:5432`, named volume `pgdata` for persistence
   - `redis` service: image `redis:7-alpine`, port `6379:6379`
   - `volumes:` section declaring `pgdata`
   - Connection strings MUST match `.env.example` values: `postgresql://gpuai:gpuai@localhost:5432/gpuai` and `redis://localhost:6379/0`

2. Update `.env.example`:
   - Keep all existing env vars and comments
   - Ensure `DATABASE_URL` and `REDIS_URL` match the Docker Compose services exactly
   - No changes needed if already matching (they do match currently)

3. Initialize Go module dependencies:
   - Run `go get github.com/jackc/pgx/v5` (for pgxpool in Plan 03)
   - Run `go get github.com/redis/go-redis/v9` (for Redis client in Plan 03)
   - This creates `go.sum` and updates `go.mod` with the dependency graph
   - These are added now so Plan 03 can import them without conflicting on go.mod

4. Run `go build ./cmd/gpuctl` to verify everything compiles (main.go does not yet import config, so it should still compile as-is).
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai && go build ./cmd/gpuctl && echo "build OK"</automated>
    <manual>Verify docker-compose.yml has postgres and redis services with correct ports</manual>
  </verify>
  <done>docker-compose.yml created with Postgres 16 and Redis 7, go.mod includes pgx/v5 and go-redis/v9, project compiles</done>
</task>

</tasks>

<verification>
1. `go vet ./internal/config/` passes with no errors
2. `go build ./cmd/gpuctl` compiles successfully
3. `docker-compose.yml` exists with postgres and redis services
4. `go.mod` contains `github.com/jackc/pgx/v5` and `github.com/redis/go-redis/v9`
5. `internal/config/config.go` exports `Config` struct and `Load` function
</verification>

<success_criteria>
- Config.Load() returns error listing all missing required env vars when they are absent
- Config.Load() returns error when INTERNAL_API_TOKEN is "change-me"
- Config.Load() returns valid Config when all required vars are set
- Docker Compose defines Postgres 16 and Redis 7-alpine with correct ports and credentials
- Go module has pgx/v5 and go-redis/v9 as dependencies
- Project compiles with `go build ./cmd/gpuctl`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
