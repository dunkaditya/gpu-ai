---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/api/middleware.go
  - internal/api/server.go
  - internal/api/middleware_test.go
autonomous: true
gap_closure: true
requirements:
  - AUTH-04

must_haves:
  truths:
    - "An HTTP request to /health from a non-loopback IP is rejected regardless of whether a valid Bearer token is provided"
    - "An HTTP request to /health from 127.0.0.1 or ::1 with a valid Bearer token succeeds with 200"
    - "An HTTP request to /health from 127.0.0.1 without a valid Bearer token is rejected with 403"
  artifacts:
    - path: "internal/api/middleware.go"
      provides: "LocalhostOnly middleware function"
      contains: "func LocalhostOnly"
    - path: "internal/api/server.go"
      provides: "Health route wrapped with LocalhostOnly + InternalAuthMiddleware"
      contains: "LocalhostOnly"
    - path: "internal/api/middleware_test.go"
      provides: "Tests for LocalhostOnly middleware"
      contains: "TestLocalhostOnly"
  key_links:
    - from: "internal/api/server.go"
      to: "internal/api/middleware.go"
      via: "LocalhostOnly wrapping health handler chain"
      pattern: "LocalhostOnly.*InternalAuthMiddleware"
---

<objective>
Add localhost-only IP restriction to internal endpoints, closing the AUTH-04 verification gap.

Purpose: The verification report found that /health is protected only by Bearer token auth. AUTH-04 requires internal endpoints to be restricted to localhost -- an external host with a valid token must still be rejected. The localhost check must be layered before the token check as defense-in-depth.

Output: Updated middleware.go with LocalhostOnly function, updated server.go route wiring, and a test file proving the behavior.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@internal/api/middleware.go
@internal/api/server.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LocalhostOnly middleware and wire into health route with tests</name>
  <files>internal/api/middleware.go, internal/api/server.go, internal/api/middleware_test.go</files>
  <action>
1. In `internal/api/middleware.go`, add import for `"net"` at the top. Add a new `LocalhostOnly` middleware function:
   - Signature: `func LocalhostOnly(next http.Handler) http.Handler`
   - Use `net.SplitHostPort(r.RemoteAddr)` to extract the IP from the request
   - If `SplitHostPort` fails, reject with `http.NotFound(w, r)` (hide endpoint existence)
   - If the extracted host is NOT `127.0.0.1` and NOT `::1`, reject with `http.NotFound(w, r)` -- use 404 (not 403) to avoid leaking that the endpoint exists to external scanners
   - If the IP is loopback, call `next.ServeHTTP(w, r)` to continue the chain
   - Keep the existing `InternalAuthMiddleware` exactly as-is (do NOT modify it)

2. In `internal/api/server.go`, update the health route registration on line 40 from:
   ```go
   s.mux.Handle("GET /health", InternalAuthMiddleware(deps.Config.InternalAPIToken, http.HandlerFunc(s.handleHealth)))
   ```
   to:
   ```go
   s.mux.Handle("GET /health", LocalhostOnly(InternalAuthMiddleware(deps.Config.InternalAPIToken, http.HandlerFunc(s.handleHealth))))
   ```
   This layers LocalhostOnly as the outermost middleware so it rejects external IPs before even checking the token. Add `"net"` to imports if not already present (it is not needed in server.go -- LocalhostOnly is in middleware.go which handles the import).

3. Create `internal/api/middleware_test.go` with tests:
   - `TestLocalhostOnly_AllowsLoopbackIPv4`: Set `r.RemoteAddr = "127.0.0.1:12345"`, verify next handler is called (200 OK)
   - `TestLocalhostOnly_AllowsLoopbackIPv6`: Set `r.RemoteAddr = "::1:12345"` (note: use `net.JoinHostPort("::1", "12345")` format which produces `[::1]:12345`), verify next handler is called
   - `TestLocalhostOnly_RejectsExternalIPv4`: Set `r.RemoteAddr = "192.168.1.100:12345"`, verify 404 response, verify next handler is NOT called
   - `TestLocalhostOnly_RejectsExternalIPv6`: Set `r.RemoteAddr = "[2001:db8::1]:12345"`, verify 404 response
   - `TestLocalhostOnly_RejectsMalformedAddr`: Set `r.RemoteAddr = "garbage"`, verify 404 response
   - `TestInternalAuthMiddleware_ValidToken`: Verify existing middleware passes with correct token (regression)
   - `TestInternalAuthMiddleware_InvalidToken`: Verify existing middleware rejects with wrong token (regression)
   - Use `httptest.NewRecorder()` and `httptest.NewRequest()` from stdlib for all tests
   - Use a simple handler that writes 200 + "ok" body as the "next" handler to verify it was/was not called
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai && go test ./internal/api/ -v -run "TestLocalhostOnly|TestInternalAuth" -count=1</automated>
    <manual>Review middleware.go to confirm LocalhostOnly uses net.SplitHostPort and checks for 127.0.0.1 and ::1 only</manual>
  </verify>
  <done>
    - LocalhostOnly middleware exists in middleware.go and rejects non-loopback IPs with 404
    - Health route in server.go is wrapped with LocalhostOnly as outermost middleware
    - All 7 tests pass (5 LocalhostOnly + 2 InternalAuthMiddleware regression)
    - `go build ./cmd/gpuctl` still compiles
    - `go vet ./...` passes
  </done>
</task>

</tasks>

<verification>
1. `go test ./internal/api/ -v` -- all tests pass
2. `go build ./cmd/gpuctl` -- binary compiles
3. `go vet ./...` -- no issues
4. Grep `internal/api/server.go` for `LocalhostOnly(InternalAuthMiddleware` to confirm wiring
5. Grep `internal/api/middleware.go` for `func LocalhostOnly` to confirm implementation exists
</verification>

<success_criteria>
- AUTH-04 satisfied: internal endpoints reject requests from non-loopback IPs with 404, regardless of token
- Defense-in-depth preserved: loopback requests still require valid Bearer token (InternalAuthMiddleware unchanged)
- Automated tests prove both layers work independently and together
- No regressions: binary compiles, vet passes, existing behavior for loopback+valid-token unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
