---
phase: 10-frontend-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/components/cloud/InstancesTable.tsx
  - frontend/src/components/cloud/InstanceDetail.tsx
  - frontend/src/app/cloud/instances/page.tsx
  - frontend/src/app/cloud/instances/[id]/page.tsx
  - frontend/src/app/cloud/instances/[id]/layout.tsx
autonomous: true
requirements:
  - DASH-05
  - DASH-08
must_haves:
  truths:
    - "Instance table rows are clickable and navigate to /cloud/instances/{id}"
    - "Instance table shows name (editable), status badge, GPU type+count, region, hourly price, uptime, SSH command copy, terminate action"
    - "Terminate button shows ConfirmDialog instead of window.confirm()"
    - "Instance name is editable inline with pencil icon hover"
    - "Empty state shows 'No instances running' with CTA link to GPU Availability"
    - "Instance detail page at /cloud/instances/[id] shows full instance information"
    - "Instance detail page shows SSH connection command with one-click copy"
    - "Uptime is calculated and displayed for running instances"
  artifacts:
    - path: "frontend/src/components/cloud/InstancesTable.tsx"
      provides: "Enhanced instances table with clickable rows, rename, uptime, confirmation dialog"
    - path: "frontend/src/components/cloud/InstanceDetail.tsx"
      provides: "Full instance detail view component"
      min_lines: 60
    - path: "frontend/src/app/cloud/instances/[id]/page.tsx"
      provides: "Instance detail page with SWR data fetching"
      contains: "useParams"
    - path: "frontend/src/app/cloud/instances/[id]/layout.tsx"
      provides: "Server metadata layout for instance detail"
  key_links:
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "/cloud/instances/{id}"
      via: "Link component on table rows"
      pattern: "Link.*instances/"
    - from: "frontend/src/app/cloud/instances/[id]/page.tsx"
      to: "/api/v1/instances/{id}"
      via: "SWR fetch"
      pattern: "useSWR.*instances/"
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "frontend/src/lib/api.ts"
      via: "renameInstance and terminateInstance calls"
      pattern: "renameInstance|terminateInstance"
    - from: "frontend/src/components/cloud/InstancesTable.tsx"
      to: "frontend/src/components/cloud/ConfirmDialog.tsx"
      via: "import ConfirmDialog for terminate action"
      pattern: "import.*ConfirmDialog"
---

<objective>
Enhanced instances table with clickable rows, inline rename, uptime display, terminate confirmation dialog, and new instance detail page.

Purpose: This is the primary management interface. Users need to see instance status at a glance, copy SSH commands quickly, rename instances for identification, and access detailed views. The detail page is essential for instances with complex connection info.
Output: Enhanced InstancesTable, new InstanceDetail component, /cloud/instances/[id] route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-CONTEXT.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-RESEARCH.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-01-SUMMARY.md

<interfaces>
From frontend/src/lib/types.ts:
```typescript
export interface InstanceResponse {
  id: string;
  name?: string;
  status: 'starting' | 'running' | 'stopping' | 'terminated' | 'error';
  gpu_type: string;
  gpu_count: number;
  tier: 'spot' | 'on_demand';
  region: string;
  price_per_hour: number;
  connection?: ConnectionInfo;
  error_reason?: string;
  created_at: string;
  ready_at?: string;
  terminated_at?: string;
}

export interface ConnectionInfo {
  hostname: string;
  port: number;
  ssh_command: string;
}
```

From frontend/src/lib/api.ts:
```typescript
export async function terminateInstance(id: string): Promise<void> { ... }
export async function renameInstance(id: string, name: string): Promise<InstanceResponse> { ... }
export const fetcher = (url: string) => fetch(url).then(r => { ... })
```

From frontend/src/components/cloud/ConfirmDialog.tsx (created in 10-01):
```typescript
// Props: title, message, confirmLabel, confirmVariant, onConfirm, onCancel, loading
```

From frontend/src/components/cloud/StatusBadge.tsx:
```typescript
export function StatusBadge({ status }: { status: InstanceStatus }) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhanced InstancesTable with clickable rows, inline rename, uptime, ConfirmDialog</name>
  <files>frontend/src/components/cloud/InstancesTable.tsx, frontend/src/app/cloud/instances/page.tsx</files>
  <action>
**IMPORTANT: Invoke the `frontend-design` skill before writing any frontend code.**

Rewrite `InstancesTable.tsx` with the following enhancements:

**Clickable rows:**
- Wrap each table row content in a Next.js `<Link href={/cloud/instances/${instance.id}}>` that covers the row
- Use `<tr>` with cursor-pointer and hover state
- Action buttons (terminate, copy) should use `e.stopPropagation()` and `e.preventDefault()` to prevent row navigation

**Inline editable name (EditableName component):**
- Default state: show `instance.name ?? instance.id.slice(0, 12)` with a pencil icon that appears on group-hover
- If instance has a name, show the truncated ID below in `type-ui-2xs text-text-dim font-mono`
- Edit state: inline input replaces the name text, auto-focused
- On blur or Enter: call `renameInstance(instance.id, newName)` then call `onRefresh()` to revalidate SWR
- On Escape: cancel editing without saving
- Use optimistic update pattern: update local state immediately, revert on error
- Import `renameInstance` from `@/lib/api`

**Uptime column:**
- Add uptime display between Cost and SSH Command columns
- Use `formatUptime` function (from RESEARCH.md): calculates elapsed time from `ready_at ?? created_at`
- Show "--" for terminated instances
- Format: "Xh Ym" for < 24h, "Xd Yh" for > 24h

**Terminate with ConfirmDialog:**
- Replace `window.confirm()` with ConfirmDialog component (from 10-01)
- State: `terminatingId` tracks which instance has dialog open
- Dialog: title "Terminate Instance", message "This will permanently destroy the instance and stop billing. This action cannot be undone.", confirmLabel "Terminate", confirmVariant "danger"
- On confirm: call `terminateInstance(id)`, close dialog, call `onRefresh()`

**Enhanced empty state:**
- "No instances running" message
- CTA button: "Browse GPU Availability" linking to `/cloud/gpu-availability` using Next.js `<Link>`
- Icon: subtle GPU/server icon in muted circle

**Mobile cards update:**
- Update mobile card layout with same enhancements: clickable card (wrap in Link), uptime display, ConfirmDialog for terminate
- Add SSH command copy with CopyButton

**SSH command one-click copy:**
- Keep existing CopyButton pattern but ensure it's prominent
- Show full SSH command in monospace code block with copy button

**Desktop table columns (in order):**
Name | GPU | Status | Region | Cost | Uptime | SSH Command | Actions

**Instances page update** (`page.tsx`):
- Pass `mutate` function down as `onRefresh` (already done, but verify)
- Ensure SWR key `/api/v1/instances` is used consistently for cache invalidation after rename/terminate
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Table rows clickable to /cloud/instances/{id}, inline rename works with pencil icon, uptime column shows calculated duration, terminate uses ConfirmDialog instead of window.confirm, empty state has CTA link to GPU Availability, mobile cards updated</done>
</task>

<task type="auto">
  <name>Task 2: Instance detail page at /cloud/instances/[id]</name>
  <files>frontend/src/components/cloud/InstanceDetail.tsx, frontend/src/app/cloud/instances/[id]/page.tsx, frontend/src/app/cloud/instances/[id]/layout.tsx</files>
  <action>
**IMPORTANT: Invoke the `frontend-design` skill before writing any frontend code.**

**Instance detail route:**

1. Create `frontend/src/app/cloud/instances/[id]/layout.tsx`:
   - Server component with `Metadata` export: `{ title: "Instance Detail" }`
   - Passes children through

2. Create `frontend/src/app/cloud/instances/[id]/page.tsx`:
   - Client component (`"use client"`)
   - Use `useParams<{ id: string }>()` to get instance ID
   - SWR fetch: `useSWR<InstanceResponse>(\`/api/v1/instances/${id}\`, fetcher, { refreshInterval: 10000 })`
   - Loading state: skeleton matching InstanceDetail layout
   - Error state: "Instance not found" with back link to /cloud/instances
   - Pass `instance` and `mutate` to `<InstanceDetail>` component

3. Create `frontend/src/components/cloud/InstanceDetail.tsx`:
   - Props: `instance: InstanceResponse`, `onRefresh: () => void`
   - Reference Lambda Labs and RunPod instance detail pages for layout inspiration

   **Layout structure (spacious, Lambda Labs style):**

   **Header section:**
   - Back link: "< Back to Instances" using `<Link href="/cloud/instances">`
   - Instance name (editable, same EditableName pattern from table -- extract as shared component or inline)
   - StatusBadge
   - Terminate button (uses ConfirmDialog)

   **Info grid** (2-3 columns on desktop, 1 on mobile):
   Cards or sections for:
   - **GPU Configuration**: GPU type, count, VRAM info, tier badge
   - **Network / Connection**: Hostname, port, full SSH command with prominent CopyButton. The SSH command should be in a large monospace code block with a copy button -- this is the primary action users take
   - **Cost & Billing**: Hourly rate, uptime duration, estimated session cost (price_per_hour * uptime_seconds / 3600)
   - **Metadata**: Region, created_at formatted, ready_at formatted, instance ID (full UUID, copyable)

   **Error display:**
   - If `error_reason` is set, show a red banner with the error message

   **Terminated state:**
   - If terminated: show terminated_at timestamp, muted styling, no terminate button, no SSH command
   - Show a "Launch New Instance" CTA linking to /cloud/gpu-availability

   **Styling:**
   - Each info section in a card: `bg-bg-card border border-border rounded-xl p-5`
   - Section labels in `type-ui-xs text-text-dim font-medium uppercase tracking-wider`
   - Values in `type-ui-sm text-text font-mono` or `type-body text-text`
   - SSH command block: larger, prominent, `bg-bg rounded-lg border border-border p-4` with monospace font
   - Generous spacing between sections (space-y-6)

4. **SWR cache consistency** (Pitfall 4 from RESEARCH):
   - After rename or terminate on detail page, invalidate BOTH:
     - `/api/v1/instances/${id}` (detail key)
     - `/api/v1/instances` (list key) using `useSWRConfig().mutate`
   - This ensures navigating back to the list shows updated data
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>/cloud/instances/[id] route renders instance detail with GPU specs, SSH command with copy button, cost/billing info, terminate with confirmation, back link to instances list, SWR cache invalidation works for both detail and list keys, terminated instances show appropriate state</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- /cloud/instances/[id] route exists in build output
- InstanceDetail component renders all instance fields
- InstancesTable rows navigate on click
- No `window.confirm()` calls remain in InstancesTable
</verification>

<success_criteria>
- Table rows navigate to /cloud/instances/{id} on click
- Pencil icon appears on hover for inline rename
- Uptime column shows calculated duration from ready_at/created_at
- Terminate action opens ConfirmDialog (no window.confirm)
- Empty state has "Browse GPU Availability" CTA link
- Instance detail page shows all info: GPU, connection, billing, metadata
- SSH command prominently displayed with one-click copy
- Detail page terminate/rename invalidates both SWR cache keys
- Terminated instances show appropriate muted state
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-03-SUMMARY.md`
</output>
