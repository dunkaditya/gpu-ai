---
phase: 10-frontend-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - frontend/src/components/cloud/SSHKeyManager.tsx
  - frontend/src/components/cloud/BillingDashboard.tsx
  - frontend/src/app/cloud/ssh-keys/page.tsx
  - frontend/src/app/cloud/billing/page.tsx
  - frontend/src/app/cloud/settings/page.tsx
autonomous: false
requirements:
  - DASH-06
  - DASH-07
must_haves:
  truths:
    - "SSH Keys page uses ConfirmDialog instead of window.confirm for delete"
    - "SSH Keys page styling matches the elevated design standard"
    - "Billing page styling matches the elevated design standard"
    - "Settings page shows meaningful content"
    - "All dashboard pages have consistent visual treatment"
  artifacts:
    - path: "frontend/src/components/cloud/SSHKeyManager.tsx"
      provides: "Polished SSH key management with ConfirmDialog"
      contains: "ConfirmDialog"
    - path: "frontend/src/components/cloud/BillingDashboard.tsx"
      provides: "Polished billing dashboard matching new design standard"
    - path: "frontend/src/app/cloud/settings/page.tsx"
      provides: "Settings page with spending limit management"
  key_links:
    - from: "frontend/src/components/cloud/SSHKeyManager.tsx"
      to: "frontend/src/components/cloud/ConfirmDialog.tsx"
      via: "import for delete confirmation"
      pattern: "import.*ConfirmDialog"
---

<objective>
Polish SSH Keys, Billing, and Settings pages to match the elevated design standard, replace remaining window.confirm() calls, and add a visual verification checkpoint.

Purpose: Ensures every page in the dashboard has consistent, production-quality design. SSH Keys and Billing already work but use basic styling and window.confirm(). Settings needs real content.
Output: Polished SSH Keys page, polished Billing page, Settings page with spending limit management, visual checkpoint.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-CONTEXT.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-RESEARCH.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-01-SUMMARY.md

<interfaces>
From frontend/src/lib/api.ts:
```typescript
export async function addSSHKey(name: string, publicKey: string): Promise<SSHKeyResponse> { ... }
export async function deleteSSHKey(id: string): Promise<void> { ... }
export async function fetchBillingUsage(period?: string): Promise<UsageResponse> { ... }
export const fetcher = (url: string) => fetch(url).then(r => { ... })
```

From frontend/src/lib/types.ts:
```typescript
export interface SSHKeyResponse { id: string; name: string; fingerprint: string; created_at: string; }
export interface UsageResponse { sessions: BillingSessionResponse[]; total_cost: number; currency: string; }
```

From internal/api/server.go (spending limit endpoints already exist):
```go
s.mux.Handle("PUT /api/v1/billing/spending-limit", authChain(...))
s.mux.Handle("GET /api/v1/billing/spending-limit", authChain(...))
s.mux.Handle("DELETE /api/v1/billing/spending-limit", authChain(...))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish SSHKeyManager, BillingDashboard, and Settings page</name>
  <files>frontend/src/components/cloud/SSHKeyManager.tsx, frontend/src/components/cloud/BillingDashboard.tsx, frontend/src/app/cloud/ssh-keys/page.tsx, frontend/src/app/cloud/billing/page.tsx, frontend/src/app/cloud/settings/page.tsx</files>
  <action>
**IMPORTANT: Invoke the `frontend-design` skill before writing any frontend code.**

**SSHKeyManager polish:**
- Replace `window.confirm()` with ConfirmDialog: title "Delete SSH Key", message "Instances using this key will no longer accept connections with it. This action cannot be undone.", confirmLabel "Delete", confirmVariant "danger"
- Track `deletingKeyId` state for which key has the confirm dialog open
- Improve visual hierarchy: make the key list cleaner with more spacious rows
- Add key count in the section header (e.g., "SSH Keys (3)")
- Add form improvements: better validation feedback, clear button states
- Ensure add key form has consistent styling with new design standard
- Keep all existing SWR patterns and API calls

**BillingDashboard polish:**
- Summary cards: add subtle border-left accent color (purple for total cost, green for active sessions)
- Active session rows: keep the green pulse dot indicator
- Improve period selector styling for consistency with new design
- Add "No spending limit set" indicator if no limit exists
- Cost values: ensure consistent `$X.XX` formatting throughout
- Keep all existing SWR patterns (60s refresh interval)

**Settings page:**
- Replace "coming soon" placeholder with real spending limit management
- Add API functions to `api.ts` if not already present:
  - `getSpendingLimit()`: GET /api/v1/billing/spending-limit
  - `setSpendingLimit(limit_cents: number)`: PUT /api/v1/billing/spending-limit
  - `deleteSpendingLimit()`: DELETE /api/v1/billing/spending-limit
- Settings page layout:
  - "Settings" heading
  - **Spending Limit section**: Card with current limit display, set/update form, remove button
    - Input in dollars (convert to cents for API)
    - Show warning: "Instances will be stopped when this limit is reached"
    - ConfirmDialog for removing limit
  - **Organization section** (placeholder): "Organization settings coming soon" (matches the "coming soon" pattern)
  - Make this a client component with SWR for spending limit data

**SSH Keys page** (`page.tsx`):
- Ensure heading says "SSH Keys" with "Add SSH Key" button in header row (consistent with instances page pattern)

**Billing page** (`page.tsx`):
- Ensure heading says "Billing" consistent with other pages
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>SSHKeyManager uses ConfirmDialog for delete, BillingDashboard has polished summary cards, Settings page shows spending limit management with set/update/remove, all pages match elevated design standard, no window.confirm() calls remain in codebase, frontend builds cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete dashboard redesign</name>
  <files>n/a</files>
  <action>
Human verifies the complete dashboard redesign visually. All code changes were made in prior tasks and plans. This checkpoint confirms design quality, interaction patterns, and overall feel match the Lambda Labs-inspired aesthetic described in CONTEXT.md.

What was built across all 4 plans: sidebar navigation with 7 items and mobile support, GPU availability card grid, enhanced instances table with clickable rows and inline rename, instance detail page, launch modal with price confirmation, polished billing/SSH keys/settings pages, confirmation dialogs throughout.
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms: all pages render correctly, design is clean and spacious, dark theme consistent, no Geist fonts, all interactive features work (navigation, launch modal, terminate dialog, rename, copy SSH, filters)</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- No `window.confirm()` calls remain in any cloud component
- All pages render with consistent design
- Settings page connects to spending limit API endpoints
</verification>

<success_criteria>
- SSH Keys delete uses ConfirmDialog
- Billing summary cards have accent styling
- Settings page manages spending limits via API
- All dashboard pages visually consistent
- Human verification confirms overall design quality
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-04-SUMMARY.md`
</output>
