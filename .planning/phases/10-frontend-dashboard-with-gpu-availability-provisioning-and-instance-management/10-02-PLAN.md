---
phase: 10-frontend-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - frontend/src/components/cloud/GPUAvailabilityTable.tsx
  - frontend/src/components/cloud/GPUCard.tsx
  - frontend/src/components/cloud/LaunchInstanceForm.tsx
  - frontend/src/app/cloud/gpu-availability/page.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "GPU availability page shows cards grouped by GPU model instead of flat table rows"
    - "Each card shows GPU name, VRAM, available count, spot price, on-demand price, region tags, launch button"
    - "Filter bar allows filtering by region and tier"
    - "Cards are sortable by price ascending/descending"
    - "Clicking launch on a card opens modal pre-filled with GPU type, region, and tier"
    - "Launch modal shows price confirmation with GPU specs (VRAM, RAM, storage) before submit"
    - "After successful launch, modal closes and user is redirected to instances list"
    - "Auto-attach all user SSH keys (no key picker in modal)"
  artifacts:
    - path: "frontend/src/components/cloud/GPUCard.tsx"
      provides: "Individual GPU availability card component"
      min_lines: 40
    - path: "frontend/src/components/cloud/GPUAvailabilityTable.tsx"
      provides: "Redesigned GPU availability page with card grid and filters"
      contains: "GPUCardData"
    - path: "frontend/src/components/cloud/LaunchInstanceForm.tsx"
      provides: "Enhanced launch modal with price confirmation and spec display"
      contains: "offering"
  key_links:
    - from: "frontend/src/components/cloud/GPUAvailabilityTable.tsx"
      to: "frontend/src/components/cloud/GPUCard.tsx"
      via: "import and render in grid"
      pattern: "import.*GPUCard"
    - from: "frontend/src/components/cloud/GPUCard.tsx"
      to: "frontend/src/components/cloud/LaunchInstanceForm.tsx"
      via: "onLaunch callback opening modal with offering data"
      pattern: "onLaunch"
    - from: "frontend/src/components/cloud/LaunchInstanceForm.tsx"
      to: "frontend/src/lib/api.ts"
      via: "createInstance call"
      pattern: "createInstance"
---

<objective>
Redesign GPU availability from flat table to card grid grouped by GPU model, and enhance the launch modal with price confirmation and GPU spec display.

Purpose: This is the primary discovery flow -- users browse GPU cards, compare prices, and launch instances. The card grid with spec display provides the informed purchasing experience described in CONTEXT.md.
Output: GPU card grid with filters/sorting, enhanced launch modal with pre-fill and price confirmation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-CONTEXT.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-RESEARCH.md
@.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-01-SUMMARY.md

<interfaces>
From frontend/src/lib/types.ts:
```typescript
export interface AvailableOffering {
  gpu_model: string;
  vram_gb: number;
  cpu_cores: number;
  ram_gb: number;
  storage_gb: number;
  price_per_hour: number;
  region: string;
  tier: 'spot' | 'on_demand';
  available_count: number;
  avg_uptime_pct: number;
}

export interface CreateInstanceRequest {
  gpu_type: string;
  gpu_count: number;
  region: string;
  tier: 'spot' | 'on_demand';
  ssh_key_ids?: string[];
}
```

From frontend/src/lib/api.ts:
```typescript
export const fetcher = (url: string) => fetch(url).then(r => { ... })
export async function createInstance(req: CreateInstanceRequest): Promise<InstanceResponse> { ... }
```

<!-- GPUCardData type to create (from RESEARCH.md) -->
```typescript
interface GPUCardData {
  gpu_model: string;
  vram_gb: number;
  cpu_cores: number;
  ram_gb: number;
  storage_gb: number;
  regions: string[];
  spot_price?: number;
  on_demand_price?: number;
  total_available: number;
  offerings: AvailableOffering[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GPUCard component and GPU availability card grid with filters</name>
  <files>frontend/src/components/cloud/GPUCard.tsx, frontend/src/components/cloud/GPUAvailabilityTable.tsx, frontend/src/app/cloud/gpu-availability/page.tsx</files>
  <action>
**IMPORTANT: Invoke the `frontend-design` skill before writing any frontend code.**

**GPUCard component** (`GPUCard.tsx`):

Create a new card component for displaying a single GPU model:
- Props: `card: GPUCardData`, `onLaunch: (offering: AvailableOffering) => void`
- Card uses `bg-bg-card border border-border rounded-xl p-5 hover:border-border-light transition-all`
- **Header section**: GPU model name in `type-h5 font-sans text-text font-bold`, VRAM badge (e.g., "80 GB" in `type-ui-xs bg-purple-dim text-purple-light rounded-full px-2 py-0.5`)
- **Specs row**: CPU cores, RAM, Storage in a horizontal layout with muted labels (`type-ui-2xs text-text-dim`) and values (`type-ui-sm text-text-muted font-mono`)
- **Pricing section**: Two columns side by side:
  - Spot: price or "--" if unavailable, with "Spot" label
  - On-Demand: price or "--" if unavailable, with "On-Demand" label
  - Prices formatted as `$X.XX/hr` in `type-ui-sm font-mono text-text`
  - Tier label in `type-ui-2xs text-text-dim uppercase`
- **Region tags**: Horizontal wrap of region pills (`type-ui-2xs bg-bg-card-hover text-text-muted rounded-full px-2 py-0.5`)
- **Availability + Launch row**: Available count (color-coded: green >5, amber 1-5, red-400 for 0) and a "Launch" gradient-btn button
- **Launch button**: When clicked, picks the best offering (cheapest available) from `card.offerings` and calls `onLaunch(offering)`
- If `total_available === 0`, show "Unavailable" instead of Launch button

**GPUAvailabilityTable redesign** (rename to keep the filename for backward compatibility):

Replace the table-based display with a card grid:
- Keep the existing SWR fetch: `useSWR<{ available: AvailableOffering[] }>("/api/v1/gpu/available", fetcher, { refreshInterval: 30000 })`
- **Grouping logic**: Use `useMemo` to group flat `AvailableOffering[]` into `GPUCardData[]` (follow the exact pattern from RESEARCH.md Code Examples -- group by gpu_model, extract spot/on-demand prices as minimums, collect unique regions, sum available_count)
- **Filter bar** (keep existing structure, but adapt):
  - Region dropdown: `<select>` with all unique regions
  - Tier toggle: segmented control (All / Spot / On-Demand) -- same pattern as current
  - Sort dropdown or toggle: by price (asc/desc)
  - Filters apply BEFORE grouping (filter flat offerings, then group)
- **Card grid**: `grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4`
- After filtering, if 0 cards: show empty state with "No GPUs match your filters" and "Clear filters" button
- **Skeleton loading**: Show 6 skeleton cards (pulsing rectangles matching card dimensions) while loading
- **Error state**: Keep existing retry pattern

**GPU Availability page** (`page.tsx`):
- Make this a client component with `"use client"`
- The page should have: heading "GPU Availability" + the GPUAvailabilityTable component
- Preserve existing structure
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>GPU availability page renders card grid grouped by GPU model, each card shows specs and both spot/on-demand prices, filter bar works for region and tier, clicking Launch on a card triggers onLaunch with offering data</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced LaunchInstanceForm with price confirmation and pre-fill</name>
  <files>frontend/src/components/cloud/LaunchInstanceForm.tsx</files>
  <action>
**IMPORTANT: Invoke the `frontend-design` skill before writing any frontend code.**

Rewrite `LaunchInstanceForm.tsx` to support two modes:

1. **Pre-filled mode** (launched from GPU card): Receives full offering data via new `offering?: AvailableOffering` prop. Shows a price confirmation summary instead of free-text inputs.
2. **Manual mode** (launched from instances page "Launch Instance" button): Same as before -- free-text GPU type, region, tier selector.

**Props update:**
```typescript
interface LaunchInstanceFormProps {
  onClose: () => void;
  onSuccess: () => void;
  defaultGPU?: string;
  defaultRegion?: string;
  offering?: AvailableOffering;  // NEW: full offering for pre-filled mode
}
```

**When `offering` is provided (pre-filled mode):**
- Show a confirmation card instead of text inputs:
  - GPU model name in `type-h4 font-sans`
  - Specs grid: VRAM, CPU cores, RAM, Storage in a 2x2 grid
  - Region displayed as tag
  - Tier (spot/on-demand) displayed as badge
  - **Price confirmation**: Large price display `$X.XX/hr` in `type-h3 font-mono text-purple-light`
  - "Estimated monthly cost: ~$X.XX" calculation below (price * 730 hours)
- GPU count selector: number input 1-8 (default 1), updates price display dynamically
- Auto-SSH-key note: Small muted text "All your SSH keys will be automatically attached"
- Submit button says "Launch Instance - $X.XX/hr"
- On submit: call `createInstance({ gpu_type: offering.gpu_model, gpu_count, region: offering.region, tier: offering.tier })` -- NO ssh_key_ids (backend auto-attaches per CONTEXT.md decision)

**When `offering` is NOT provided (manual mode):**
- Keep existing free-text inputs for GPU type, region, tier, GPU count
- But improve: if `defaultGPU` and `defaultRegion` are provided, pre-fill them
- No price confirmation (we don't have offering data)

**After successful launch:**
- Close modal (`onClose()`)
- Call `onSuccess()` for SWR revalidation
- Use `router.push('/cloud/instances')` from `next/navigation` to redirect to instances page (per CONTEXT.md: "redirect to instances list")

**Shared improvements (both modes):**
- Use ConfirmDialog-style error display (red banner)
- Loading state: disabled button with "Launching..." text
- Escape key closes modal
- Backdrop click closes modal (unless loading)
  </action>
  <verify>
    <automated>cd /Users/adityareddy/Repo/gpu-ai/frontend && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Launch modal shows price confirmation with GPU specs when opened from a card, supports manual mode when opened from instances page, GPU count updates price dynamically, successful launch redirects to instances list, builds cleanly</done>
</task>

</tasks>

<verification>
- `cd frontend && npx next build` passes
- GPUCard component exported from GPUCard.tsx
- GPUAvailabilityTable uses card grid layout (not table)
- LaunchInstanceForm accepts `offering` prop
- Grouping logic produces GPUCardData with spot and on-demand prices
</verification>

<success_criteria>
- GPU availability page shows cards grouped by GPU model (not flat table rows)
- Each card displays: GPU name, VRAM, CPU, RAM, storage, spot price, on-demand price, regions, available count
- Filter bar supports region dropdown and tier toggle
- Clicking Launch on a card opens modal with that GPU's specs and price pre-filled
- Launch modal shows price confirmation: hourly cost, estimated monthly, GPU specs
- GPU count selector dynamically updates displayed price
- After successful launch, user is redirected to /cloud/instances
- All user SSH keys auto-attached (no key picker)
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-dashboard-with-gpu-availability-provisioning-and-instance-management/10-02-SUMMARY.md`
</output>
