---
phase: 04.3-auth-idempotency-edge-cases
verified: 2026-02-25T09:00:00Z
status: passed
score: 3/3 must-haves verified
re_verification: false
gaps: []
human_verification:
  - test: "Create two users in the same org after migration runs"
    expected: "Both users created without constraint violation"
    why_human: "Migration must be applied to a live DB; cannot verify schema state without running migrate.py"
  - test: "Send POST /api/v1/instances with Idempotency-Key for a brand-new org (no previous DB record)"
    expected: "Returns 201 with instance data; no 500 error; retrying with same key returns same 201 response"
    why_human: "Requires live Clerk JWT + PostgreSQL instance with migration applied"
---

# Phase 4.3-01: Auth & Idempotency Edge Cases Verification Report

**Phase Goal:** Fix two structural bugs: the email UNIQUE constraint that blocks multi-user organizations, and the idempotency middleware race condition with org creation
**Verified:** 2026-02-25T09:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Multiple distinct Clerk users in the same organization can be created without constraint violations | VERIFIED | `ALTER TABLE users DROP CONSTRAINT users_email_key` and `ALTER COLUMN email DROP NOT NULL` present in `20250225_v4_auth_idempotency_fixes.sql` lines 22 and 30. `EnsureOrgAndUser` uses `ON CONFLICT (clerk_user_id) DO UPDATE` — dedup is on Clerk user ID, not email. `User.Email` changed to `*string` (nullable). |
| 2 | Idempotency-Key header works correctly even for a new organization's first POST request | VERIFIED | `IdempotencyMiddleware` in `internal/api/idempotency.go` lines 71–86: when `GetOrgIDByClerkOrgID` returns `db.ErrNotFound`, it eagerly calls `dbPool.EnsureOrg(ctx, claims.OrgID)` before falling through to normal idempotency logic. The org is created in-band so the idempotency key is always scoped and stored — no pass-through gap. |
| 3 | Instance records store the internal user UUID (not Clerk user ID) in user_id column | VERIFIED | `handlers.go` line 122 destructures `orgID, userID, err := s.db.EnsureOrgAndUser(...)` and line 146 sets `UserID: userID`. `EnsureOrgAndUser` now returns `(orgID string, userID string, err error)` with `RETURNING user_id` on the user upsert query (line 96 of `organizations.go`). `ProvisionRequest.UserID` comment in `engine.go` line 25 explicitly documents "internal user UUID from users.user_id (not Clerk user ID)". |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Provides | Level 1 (Exists) | Level 2 (Substantive) | Level 3 (Wired) | Status |
|----------|----------|-----------------|----------------------|-----------------|--------|
| `database/migrations/20250225_v4_auth_idempotency_fixes.sql` | Schema fix dropping email UNIQUE constraint and making email nullable | EXISTS | Contains `DROP CONSTRAINT users_email_key`, `DROP NOT NULL`, `DROP INDEX IF EXISTS idx_users_email` — not a stub | Migration runner (`tools/migrate.py`) applies files in order; file is self-contained SQL with header comments | VERIFIED |
| `internal/db/organizations.go` | EnsureOrg function and refactored EnsureOrgAndUser returning both orgID and userID | EXISTS | `EnsureOrg` at line 52 (27-line implementation with INSERT + SELECT), `EnsureOrgAndUser` at line 81 returning named `(orgID string, userID string, err error)` with `RETURNING user_id` | Called by `idempotency.go` (line 76) and `handlers.go` (line 122) | VERIFIED |
| `internal/api/idempotency.go` | Idempotency middleware handling org-not-found with eager EnsureOrg | EXISTS | Full middleware implementation with responseCapture, ErrNotFound branch calling `dbPool.EnsureOrg`, `CreateIdempotencyKey`, `CompleteIdempotencyKey`, and race-condition re-lookup | Registered on router (pre-existing wiring from Phase 4); `EnsureOrg` call at line 76 properly follows `errors.Is(err, db.ErrNotFound)` check | VERIFIED |
| `internal/api/handlers.go` | Handler using internal user UUID from EnsureOrgAndUser | EXISTS | `handleCreateInstance` at line 109 destructures three return values from `EnsureOrgAndUser` and passes `userID` (not `claims.UserID`) to `provReq.UserID` | `provReq.UserID = userID` wired at line 146; no other callers of `EnsureOrgAndUser` exist in codebase | VERIFIED |

### Key Link Verification

| From | To | Via | Pattern | Status |
|------|----|-----|---------|--------|
| `internal/api/idempotency.go` | `internal/db/organizations.go` | EnsureOrg call when GetOrgIDByClerkOrgID returns ErrNotFound | `dbPool.EnsureOrg` | WIRED — line 76: `internalOrgID, err = dbPool.EnsureOrg(ctx, claims.OrgID)` inside `errors.Is(err, db.ErrNotFound)` branch |
| `internal/api/handlers.go` | `internal/db/organizations.go` | EnsureOrgAndUser returning both orgID and userID | `EnsureOrgAndUser.*orgID.*userID` | WIRED — line 122: `orgID, userID, err := s.db.EnsureOrgAndUser(ctx, claims.OrgID, claims.UserID, "")` |
| `internal/api/handlers.go` | `internal/provision/engine.go` | provReq.UserID set to internal user UUID | `UserID.*userID` | WIRED — line 146: `UserID: userID` in ProvisionRequest struct literal |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| AUTH-03 | `04.3-01-PLAN.md` | Users can only access instances belonging to their organization | SATISFIED | Root cause was that a second user in an org could not be created at all (email UNIQUE blocked it). Migration v4 drops `users_email_key` and makes email nullable. `EnsureOrgAndUser` now correctly upserts multiple users in the same org via `ON CONFLICT (clerk_user_id)`. Org-scoped queries (`ListInstances`, `GetInstanceForOrg`) were already correct — they are unchanged and still enforce org isolation. |
| API-11 | `04.3-01-PLAN.md` | POST /api/v1/instances accepts Idempotency-Key header to prevent duplicate instance creation on network retries | SATISFIED | Idempotency middleware previously returned 500 for a new org's first request because `GetOrgIDByClerkOrgID` returned `ErrNotFound`. Now the middleware eagerly calls `EnsureOrg` on `ErrNotFound`, then falls through to normal idempotency logic. The idempotency key is always stored via `CreateIdempotencyKey` + `CompleteIdempotencyKey`, ensuring replays work on retries. |

**Traceability check:** REQUIREMENTS.md Traceability table maps `AUTH-03 → Phase 4.3 (Complete)` and `API-11 → Phase 4.3 (Complete)`. Both match the PLAN frontmatter. No orphaned requirements found.

### Anti-Patterns Found

| File | Pattern | Severity | Notes |
|------|---------|----------|-------|
| None found | — | — | No TODO/FIXME/placeholder comments, no empty implementations, no stub return values in any modified file |

### Human Verification Required

#### 1. Multi-user org creation after migration

**Test:** Apply migration v4 (`python tools/migrate.py`) then use Clerk test credentials to create two users in the same org via POST /api/v1/instances (or direct DB INSERT with two distinct `clerk_user_id` values but the same `email` value, e.g., `test@example.com`).
**Expected:** Both users are inserted successfully; no `unique constraint "users_email_key"` error.
**Why human:** Migration must be applied to a running PostgreSQL instance. The constraint drop cannot be verified by static analysis alone — the SQL is correct but only an applied migration confirms the live schema matches.

#### 2. First-request idempotency for a new org

**Test:** With a Clerk org that has never made an API request, send `POST /api/v1/instances` with a valid `Idempotency-Key: test-key-001` header. Then send the identical request again with the same key.
**Expected:** First request returns 201 (instance created). Second request returns 201 with the identical body (replay from idempotency store). No 500 errors on either request.
**Why human:** Requires a live Clerk JWT, running PostgreSQL with v4 migration applied, and a RunPod API key. The middleware code logic is fully verified; the integration path needs end-to-end confirmation.

### Gaps Summary

No gaps. All three observable truths are verified by direct code inspection:

1. The email UNIQUE constraint drop and nullable change are present and syntactically correct in the migration file.
2. The idempotency middleware correctly handles the `ErrNotFound` case with eager `EnsureOrg` — no pass-through gap, no 500 on first request.
3. The handler correctly destructures the new three-value return from `EnsureOrgAndUser` and passes the internal user UUID (not the Clerk user ID string) to the provisioning engine.

Build compiles cleanly (`go build ./...` exits 0). All tests pass (`go test ./...` — all test packages pass). Both task commits (`d3040ac`, `f753658`) are present in git history.

The two human verification items are integration tests that cannot be automated without a live environment; they do not represent code gaps.

---

_Verified: 2026-02-25T09:00:00Z_
_Verifier: Claude (gsd-verifier)_
